# 山西证券 PTrade 三层策略 —— 上线前核对清单

> 本策略文件：`山西PTrade_三层量化策略.py`
> 平台：山西证券 PTrade（恒生系白标）
> 默认 `TRADE_ENABLED = False`：先信号模式跑 3~5 个交易日核对日志，再模拟盘，最后小资金实盘。
> **本版来源**：由 `长线股三层量化策略_国金PTrade/国金PTrade_三层量化策略.py` 券商适配而来，交易逻辑一致。
>
> ✅ **API 口径已定稿（2026-09-21）**：已对照官方《山西证券的API文档.docx》（提取文本 `山西证券_API文档_提取.txt`，173,835 字符）逐条核对，**原 9 项待核 ☐ 全部闭环**。
> 核对过程中发现并修复 **1 处 P0 级静默缺陷**（`get_positions()` 返回形态误判 → 持仓读取全丢），并同步修复国金版。详见 §六。

---

## 〇、山西版 vs 国金版：差异一览

| 维度 | 国金版 | 山西版（本文件） |
|---|---|---|
| 文件 / 日志标识 | `国金PTrade_三层量化策略.py` | `山西PTrade_三层量化策略.py`，初始化日志加 `★山西PTrade V1.0★` 指纹（防"跑错版本"） |
| 交易逻辑 | 三层架构 + 风控闸门 + 参数 | **完全一致** |
| 接口口径 | 对照《国金PTrade的API文档》定稿 | **对照《山西证券的API文档》逐条核对，结论与国金一致** |
| `get_history` 含当日 bar | 国金文档未特别说明 | 山西文档 `include` 默认 `False`（不含当前周期）→ 盘前取数更无影响 |
| `handle_data` 的 `data` | 回测可取盘中 data | 山西文档明确 `data[security]` 返回 **BarData**，可取 `.price/.close` → L3 具备运行条件；实盘仍以 `[口径]` 日志确认 |

---

## 一、山西证券官方文档口径（逐条对照《山西证券的API文档.docx》）

| # | 项 | 文档原文口径（含行号） | 对本策略的影响 |
|---|---|---|---|
| 1 | `get_history` 签名 | `get_history(count, frequency='1d', field='close', security_list=None, fq=None, include=False, fill='nan', is_dict=False)`（文档 L1124） | **与国金完全一致**；`_hist_lists` / `_vwap` 无需改动 |
| 2 | `get_history` 返回形态 | 单只**字符串**入参 → `pandas.DataFrame`（行=datetime，列=字段名，L1162）；传 list 时 3.5 与 3.11 形态不同（L1170/L1188） | 本策略**一律传单只字符串** → 两版本下同为 DataFrame，跨版本行为一致 |
| 3 | `include` 参数 | 是否包含当前周期，**默认 `False`**（L1152） | 盘前取数末根=昨收，L1/L2 指标口径正确 |
| 4 | 分钟线频率 | 明确支持 `1m/5m/15m/30m/60m/120m/1d/1w/mo/1q/1y`（L1136） | L3 的 `_vwap` 可用；回测若不供 1m 则 VWAP=None、L3 自动关闭 |
| 5 | `get_snapshot(security)` | 参数可为单只 str 或 list；返回 **`dict[str:dict]`**（键=代码），`last_px`=最新成交价（L1676-1695） | `_cur_price` 用 `snap.get(code, snap)` 取值 → **已兼容**；仅**交易**模块可用 |
| 6 | `get_positions(security)` | 返回 **`dict[str:Position]`**（键=代码，值=Position 对象；L2404/L2419） | ⚠️ **本项即 P0 缺陷来源**，已修复（见 §六） |
| 7 | Position 对象字段 | `sid` / `enable_amount`(可用) / `amount`(总持仓) / `last_sale_price`(最新价) / `cost_basis`(成本)（L4909-4913） | `_pos_to_dict` 对象分支字段**完全匹配** |
| 8 | `get_all_positions()` | 返回 **`list[dict]`**：`stock_code`(6 位无后缀)/`current_amount`/`enable_amount`/`last_price`/`cost_price`（**仅交易**，L2429） | 本策略未使用；`_pos_to_dict` 的 dict 分支正为此保留 |
| 9 | `context.portfolio` | `cash` / `positions`(dict[str:Position]) / `portfolio_value` / `positions_value` / `capital_used`（L4875-4879） | `_account()` 读取的三个字段**均存在** |
| 10 | 时钟 | `context.blotter.current_dt`（L983/L1073 等多处） | `_dt_now` 主路径正确 |
| 11 | `data[code]` | 返回 **BarData** 对象，可取 `.price`(最新价)/`.close`/`.low`/`.high`/`.symbol`/`.datetime`（L4844-4846）；亦支持 `data[code]['close']`（L2834） | `_cur_price` 的 `data[code].price` 路径正确 |
| 12 | `order_target_value` | `order_target_value(security, value, limit_price=None)`；`value`=目标市值（L2838） | 两参调用合法（不传 limit_price 时以快照最新价报单，快照失败会记入日志） |
| 13 | Python 版本 | 文档明确「**python3.5、python3.11 版本均支持**」（L1161/L1170） | 策略为 3.5 兼容写法，两版本均可运行 |
| 14 | 复权 | `fq`：`pre`/`post`/`dypre`/`None`（默认 **None 不复权**，L1151） | 需按平台实际默认口径核对（见 §五） |
| 15 | 并发限制 | `get_history` / `get_price` **不支持多线程同时调用**，勿在 `run_daily`/`run_interval` 与 `handle_data` 同一时刻调用（L1133） | 本策略仅于 `before_trading_start` 与 `handle_data` 内调用，不并发 ✅ |
| 16 | `set_commission` | 仅**回测**可用（L231） | 策略未调用（正确） |

> 文档中**未出现 `get_account()`** —— 与国金一致，PTrade 无此接口，账户信息只在 `context.portfolio`。

---

## 二、接口口径核对表（山西文档已核，9 项全部闭环）

| # | 项 | 本策略写法 | 山西文档核对结果 |
|---|---|---|---|
| 1 | 股票代码后缀 | `.SS/.SZ/.BJ`（`_suffix` 自动补，指数原样） | 【已确认】文档示例 `'600570.SS'` / `'000001.SZ'` |
| 2 | 历史行情 `get_history` | `(count, frequency, field, security_list)`，无 `df` 参数，单只字符串返回 DataFrame | 【已确认】L1124 签名与返回形态一致，**无需改动** |
| 3 | 持仓 | `get_positions()` → **dict[str:Position]**，经 `_pos_to_dict` 统一映射 | 【已确认·已订正】原按 list[dict] 处理**有误**，已修复（§六） |
| 4 | 账户信息 | 无 `get_account()`；用 `context.portfolio.portfolio_value`/`cash`/`positions_value` | 【已确认】文档 Portfolio 对象列出同名字段 |
| 5 | 下单 | `order_target_value(code, value)`，value=目标市值 | 【已确认】L2838 签名兼容 |
| 6 | 盘中现价 | `data[code].price` → `obj['close']` → `get_snapshot(code)['last_px']` | 【已确认】BarData `.price` + 快照 `last_px` 均正确 |
| 7 | 分钟线 | `get_history(T_VWAP_WIN, '1m', ['close','volume'], code)`，同分钟缓存 | 【已确认】文档支持 1m 频率 |
| 8 | 回调名 / 时钟 | `initialize`/`before_trading_start`/`handle_data`/`after_trading_end`；`context.blotter.current_dt` | 【已确认】四回调与时钟属性均见文档 |
| 9 | 指数代码 | `000300.SS`（沪深300） | 【已确认】文档统一用 `.SS` 后缀，`get_history` 支持指数取数 |

> **最快的口径自检**：信号模式首跑后 grep 日志，`[hist]` / `[acct]` / `[pos]` 告警**应为 0 条**。
> 另：`set_commission` 只在**回测**可用，交易接口不存在，策略里未调用（正确）。

---

## 三、本策略已落实的硬规则

- [x] Python 3.5 / 3.11 双兼容：无 f-string、无类型注解、无 `os` 模块、无外网请求
- [x] `initialize` 内 `set_universe(STOCKS)`：注册标的后 `handle_data` 的 `data` 才带个股行情（缺则 L3 整段失效）
- [x] T+1：所有卖出走 `enable_amount`，`enable_amount == 0` 不回退到 `current_amount`（不发废单）
- [x] 涨跌停按板区分：30/68→20%、8/4→30%、ST→5%、其余 10%
- [x] 成本含印花税：卖出单边 0.05%（2023-08-28 起减半）；佣金双边 0.025%（万2.5）；滑点 0.05%（回测建模假设）
- [x] 回测当日价口径：L1/L2 信号在 `before_trading_start` 用**已完成数据**（末根=昨收）；L3 盘中价显式标注来源 `[口径]`
- [x] 盈亏比自洽：`L2_STOP_PCT(7%) <= L2_TAKE_PCT(14%) * 0.5` → 自检 PASS
- [x] 两段式止损已**弃用**：统一单段一次性清仓 + 策略自有成本价 `entry_px`（防平台摊薄口径改写）
- [x] 下单前先快照持仓（`poss = _get_positions()` 在前，`order_target_value` 在后），规避"下单后持仓被引擎清零"
- [x] 组合级风控：三级断路器（-8%/-12%/-18%）+ 沪深300 均线市场开关；断路器在 `_do_buy_value` 中**独立拦截所有方向性买入**（L1 季度再平衡补仓也受控；L3 日内T回补不走此函数）
- [x] 市场开关**双向可复位**：短均线(60)回归即恢复 L2；长均线(120)回归且连续 `MKT_RESET_CONFIRM_DAYS` 日（默认 1）后档位归零，再次跌破可再减仓；均线数据缺失时维持当前档位（`_index_below_ma` 三态返回）
- [x] 行业上限约束（`SECTOR_MAX=30%`）：`_do_buy_value` 下单前按 `SECTOR_OF` 聚合细分行业市值占比，超阈拒单/减额；L3 日内T 豁免；未配置行业标的一次告警
- [x] 单票上限（`SINGLE_MAX=10%`）+ L1 最短持有（`L1_MIN_HOLD_DAYS=5`）均已生效
- [x] 换手预算：`MAX_ANNUAL_TURNS` = 年度成本预算 ÷ 单回合成本（≈7.5 回合/年）；`TURNOVER_MIN_DAYS=20` 防早期外推；换手判定只在 L2/L3 入场处
- [x] **持仓读取口径（2026-09-21 新增）**：`_get_positions()` 按 `dict.items()` 取键值对（`get_positions()` 返回 `dict[str:Position]`）；`_pos_to_dict(p, code_hint)` 支持用字典键兜底代码；兼容 list 形态与 Position 对象
- [x] 任何接口降级都 `log.warning`，绝不静默换口径（`_hist_lists` / `_cur_price` / `_get_positions` / `_account` 全有告警）
- [x] L3 日内T 闭环：只卖可卖量、当日必回补（`14:50` 强制平），**不增隔夜仓、不碰 L1 核心**

---

## 四、上线前手动核对动作

1. **信号模式跑 3~5 日**：`TRADE_ENABLED=False`，看日志是否出现预期 `[signal-buy]`/`[信号-卖]`，以及 `[口径]`（若 L3 关说明无盘中价，正常）。
2. **口径自检**：grep 日志统计 `[hist]`/`[acct]`/`[pos]` 告警条数，**应全为 0**；若不为 0，说明某接口与文档口径有出入，需按告警定位。
3. **核对 T+1 拦截**：制造"当日买入"场景，确认打印 `可卖为0(T+1未交收)...顺延次日`，无废单。
4. **核对成本预算告警**：制造高频触发，确认 `[换手预算]` 告警并产生暂停。
5. **核对断路器**：模拟组合回撤 >8%/12%/18%，确认分级减仓日志与动作；确认断路器触发后 L1 季度再平衡不再补仓。
6. **L1 季度再平衡**只在第 60 个交易日触发一次，确认不会每天调仓。
7. **核对行业上限**：制造某细分行业（如"集成电路设计"=兆易+澜起）接近 30% 场景，确认 `[sector-cap]` 减额/拒单；L3 不受影响。
8. **核对 L1 最短持有**：对已持仓 <5 日的票制造"低配"场景，确认打印 `[L1] ... 持仓不足5日，跳过再平衡追买`。
9. **核对市场开关复位**：指数回到 120 日线上方连续 `MKT_RESET_CONFIRM_DAYS` 日后打印档位复位；此后**再次跌破**应能再砍两成。
10. **核对 L3 退化**：确认 `[口径] 盘中价来源=` 是 `data` 还是 `snapshot`；若 L3 被关闭，L1/L2 是否正常（应正常）。
11. **核对持仓读取（本次 P0 修复项，务必验）**：`TRADE_ENABLED=True` 后，确认 `_daily_risk_scan` / `_t_once` 能识别到真实持仓（日志出现持仓相关动作，而非恒空）；对照 `get_positions()` 原始返回，确认标的代码与数量一致。
12. **小资金实盘**：`TRADE_ENABLED=True`，单票极小仓，盯 1~2 周无异常再放大。

---

## 五、仍需平台侧 / 实盘确认（文档无法回答）

- **复权口径**：文档 `fq` 默认 `None`（不复权）。L2 阈值（MA20 下方 6%、量比 0.8、RSI 35–55）对复权方式敏感，需确认山西平台默认复权与选股时口径一致。
- **费率**：印花税 0.05% / 佣金 万2.5 / 滑点 0.05% 为现行估算，请按山西证券实际费率校正 `STAMP_TAX/COMM_RATE/SLIPPAGE`。
- **1m 数据可得性**：文档声明支持 1m，但回测档位是否实际提供需实跑验证；缺则 L3 自动关闭（已做降级）。
- **`handle_data` 的 `data` 实况**：文档声明提供 BarData，实盘以 `[口径] 盘中价来源=` 日志确认为准。
- **行业表可动态化**：文档提供 `get_industry_stocks`（研究/回测/交易）与 `get_Ashares`，可替换手工 `SECTOR_OF`；替换时需双写 key（纯数字 + 带后缀）。
- **持仓数据同步周期**：`Position` 对象在**交易**场景默认每 **6 秒**与柜台同步（`update_time` 记录），`order_target_value` 存在柜台时滞——策略已用 `buy_codes` 去重缓解，实盘需观察。

> ⚠️ 本报告基于量化 + 定性研究，**非买卖建议**。自动化策略仍需人工兜底开关：极端行情、停牌、平台故障、接口字段变更都需人工介入。

---

## 六、P0 缺陷修复记录（2026-09-21）

### 缺陷：`get_positions()` 返回形态误判 → 持仓读取全丢

**现象（静默、无报错）**：`_get_positions()` 原实现为：

```python
poss = get_positions()
if poss:
    for p in poss:          # ← 若 poss 是 dict，这里遍历到的是"字符串键"
        d = _pos_to_dict(p)
        ...
    return out
```

文档明确 `get_positions()` 返回 **`dict[str:Position]`**。对 dict 做 `for p in poss` 只会得到**代码字符串**（如 `'600570.SS'`）；`_pos_to_dict('600570.SS')` 走对象分支、`getattr(str, 'sid', '')` 取空 → `code=''` → `_canon('')` 为空 → `continue` → **out 恒为 `{}`**。
且因 `poss` 非空为真，`return out`（空字典）直接返回，**退化通道 `context.portfolio.positions` 永不可达**。

**连锁后果（全部静默，无一条告警）**：
- `_daily_risk_scan` 遍历空持仓 → 止盈 / 超买 / 移动止盈**永不触发**；
- `_intraday_hard_stop` 同上 → 日内硬止损**永不触发**；
- `_t_once` 找不到持仓 → **L3 日内T 全程空转**；
- `_apply_sector_cap` 聚合行业市值恒为 0 → **行业上限 30% 永不生效**；
- `_l1_rebalance` 见 `cur_val=0` → 季度再平衡重复按目标市值补仓，仓位与单票上限判断失真。

**为何此前未被发现**：国金首跑为**信号模式**（`TRADE_ENABLED=False`）零成交、无持仓，该缺陷被完全掩盖；`[pos]` 告警只在 `get_positions()` **抛异常**时打印，形态错误不抛异常。

**修复**（山西版与国金版同步）：
1. `_get_positions()` 改为 `isinstance(poss, dict) → list(poss.items())`，list 形态按下标取值；`out` 非空时才提前返回，否则保留 `context.portfolio.positions` 退化通道；
2. `_pos_to_dict(p, code_hint="")` 新增 `code_hint`，在 Position 内代码缺失时用字典键兜底（文档说明键可能是 `'600570.XSHG'`，`_canon` 已能处理四位后缀）。

**影响评估**：这是**双端（山西 + 国金）共同存在**的 P0 级缺陷——在实盘会**完全废掉风控与日内T**。修复后 `py_compile` 通过，两版均已落盘。

**残留验证动作**：见 §四 第 11 项（必须以 `TRADE_ENABLED=True` 的成交模式确认持仓可被读到）。

---

## 附：本版未包含的内容

- **回测日志与复盘**：国金目录下的 `2026-09-21 224654 ... 长线波段.txt` 与 `首跑回测复盘_20260921.md` 是**国金平台**的运行结果，未复制到本目录。山西版需在山西平台自行回测，并把日志规范命名归档（`回测_<预设>_<YYYYMMDD>.txt`，防被下次回测静默覆盖）。
- 首跑务必设 `TRADE_ENABLED = True`（否则零成交，收益与风控都验证不到——信号模式只能验接口与信号）。


---

## 七、V1.1 修复与重跑核对（2026-09-22）

### 背景：首跑「真下单模式」54 笔委托 100% 被拒

山西版首跑把 `TRADE_ENABLED` 设为 **True**（真下单模式），**54 笔买入委托全部被平台拒单**、全期零成交，
总资产恒定 100,000 元、回撤恒 0.00%。两个根因正好把 54 笔对半劈开，**数字完全闭合（误差 0）**：

| 平台报错 | 笔数 | 根因 |
|---|---|---|
| `股票委托数量为0, 委托取消` | **37** | 单票预算买不起 1 手 |
| `xxx的订单不在交易时间段内，下单失败` | **17** | 下单时点全在 **08:30 盘前** |
| 合计 | **54** = `[统计] buy: 54 笔` | — |

详见同目录 `回测复盘_山西PTrade_20260922.md`。

> ⚠️ **版本漂移提示**：首跑日志显示 `TRADE=True`，而当时本地留存的文件是 `TRADE_ENABLED = False`。
> 上线前务必确认平台上跑的**就是**当前这份 V1.1 文件 —— 核对 `[初始化]` 行是否带 `★V1.1★` 指纹。

### 官方文档给出的三条硬契约（本次新确认，此前未核实）

1. **`order_target_value(security, value, limit_price=None)` 成功返回 Order id(str)，失败返回 None。**
   → 委托成败**可精确判定**，不必靠"跑完没崩"推断。
2. **`handle_data` 执行时点**：**分钟级**策略在回测下为 **9:31–15:00**；**日线级**策略为 **15:00**（已到收盘边界）。
   → 本策略依赖分钟级（L3 日内T 需 `T_OPEN=09:45`），**回测/实盘周期必须设为分钟级**，
   否则所有委托都无法执行（V1.1 已加 error 级 `[周期告警]` 主动暴露）。
3. **`order_target_value` 在交易场景下会重复下单**：柜台持仓同步有约 6 秒时滞，或首笔未成交且未撤单时，
   引擎不会计算在途委托数量。官方明确要求"增加订单和持仓同步的管理"。
   → V1.1 已在下单前用 `get_open_orders(code)` 做在途委托去重。
   （另注：该接口**委托股票时取整 100 股**，科创板需 200 股，策略层 `_lot_min` 已自行处理。）

### V1.1 变更清单（11 处，两版同步，`py_compile` 通过）

| # | 缺陷 | 修复 |
|---|---|---|
| D9 | 五条下单路径全挂 `before_trading_start`(08:30) → 17 笔被拒 | 新增统一出口 `_submit_order()`：盘前入队 `g["pending_orders"]`，`handle_data` 首个可交易 tick 由 `_flush_pending()` 出队执行 |
| D10 | 单票预算 < 1 手仍发 0 股委托 → 37 笔被拒 | `_do_buy_value` 新增整手可行性预检（`_lot_cost`），不足则打 `[买不起]` 并跳过 |
| D11 | 平台拒单不抛异常 → 无条件写 `buy_day`，产生「幽灵建仓日」 | 新增 `_mark_bought()`，**仅在委托受理后**写 `buy_codes`/`buy_day`/换手计数；被拒则回滚 |
| D12 | `[统计] buy: N 笔` 是委托意图数，非成交数 | 拆成 `[统计] 委托口径：已提交X 被平台拒Y 有效Z` |
| D13 | 委托失败无熔断，会日复一日重复下单 | `_note_reject()`：同一标的连续 ≥3 次被拒 → `[熔断]` 暂停该标的（L1 再平衡时解除）；下单前 `_has_open_order()` 去重 |
| — | 高价股买不起导致名义仓位虚高 | 新增 `_redistribute()` **资金再分配**：买不起 1 手的预算顺延给买得起的标的 |
| — | 结构性上限无人知晓 | 新增 `_capital_fit_report()` **资本适配自检**，启动即打印可买清单 / 实际可落实仓位 / 资金利用率 |
| — | 未校验可用资金 | `_do_buy_value` 新增 `context.portfolio.cash` 预检，留 2% 缓冲 |
| — | L2 等权 2.5% 必然买不起 1 手 | `_l2_daily` 改为「先扫信号、再按当日信号数分摊 L2 层额度」 |
| — | 卖出同样受盘前时点限制 | `_do_sell` 改走统一出口，并加在途委托去重 |
| — | 周期配错时静默失效 | `handle_data` 增加 error 级 `[周期告警]` |

### 离线校验器（新增，可复用）

`D:\workbuddydata\...\verify_v11.py` —— mock 掉 PTrade 接口后直接驱动策略函数，**43 项断言全通过**，覆盖：
交易窗口判定、科创板/主板整手成本、整手预检、盘前入队、资金再分配金额守恒、L1 端到端出队、
序列化后 `order_target_value` 调用次数、**拒单不写 buy_day**、连续拒单熔断、资本适配自检输出。

### 重跑必须核对的 8 项

1. **`[初始化]` 行出现 `★V1.1★` 指纹** —— 确认平台跑的是本版；
2. **`[资本适配-关键]` 行**：确认能看到「可买标的数 × 单票上限 = 最大仓位」这一结构性结论（10 万本金 / 16 只标的场景为 **6 只 × 10% = 60%**）；
3. **`[交易执行] 出队盘前委托 N 笔` → `完成：受理N 被拒0`** —— 验证 D9；
4. 不再出现 `订单不在交易时间段内，下单失败`；
5. 不再出现 `股票委托数量为0, 委托取消`；高价股改为 `[买不起] xxx 预算A < 1手成本B`；
6. **L1 首日建仓应达 6 笔且合计 ≈ 58% 净值**（资金再分配生效；修复前只能落实 23.2%）；
7. **出现真实持仓后**：`[盘前] 总资产` 开始波动、回撤不再恒 0.00%、`[L2-卖]`/`[L2硬止损]`/`[T]` 类日志开始产生、
   `[sector-cap]` 在半导体系（中微+拓荆）或集成电路设计（兆易+澜起）接近 30% 时触发；
8. **`[统计] 委托口径`** 三数字自洽，被拒率应接近 0（若仍 > 0，看 `[委托被拒]` 给出的原因）。

### ⚠️ 尚未解决、需要决策的结构性问题

**10 万本金下组合最大仓位只有 60%**（可买标的 6 只 × 单票上限 10%），名义目标 83%，资金利用率仅 72%。
且 **L2 标的池与 L1 完全重叠**，L1 经资金再分配后单只约 9.7%，留给 L2 的单票空间合计仅约 **1.3%**（L2 名义目标 25%）。

三条出路（V1.1 的 `[资本适配-建议]` 也会在日志中提示）：

1. **放大本金** —— 50 万时 L1 单票 1.8 万，16 只里 15 只可建仓（仅茅台除外）；
2. **压缩标的池** —— 剔除高价股，保留能建仓的 6~8 只；
3. **调高 `SINGLE_MAX`** —— 当前 10%，提到 20% 可让 L2 有独立空间，代价是分散度下降。

**资金再分配只能解决「预算被高价股作废」，解决不了「单票上限 × 可买标的数」这个总量硬顶。**

## 八、V1.2 修复与验证（2026-09-22）

### 背景
V1.1 打通交易链路后，在 **30 万本金真下单模式** 重跑，暴露三层 P0 + 三个次级缺陷
（见《回测复盘_山西PTrade_V1.1_20260922.md》）。本版一次性修复 D16~D21。

### 缺陷与修复对照
| 编号 | 缺陷 | 根因 | 修复 |
|---|---|---|---|
| D16 | L1 核心仓被 L2 出场规则反复清掉（07-07 建4只次日清3只） | `_daily_risk_scan`/`_intraday_hard_stop` 用 `code in L2_SET` 判定，而 L2 池⊆L1 池 | 加 `owner_layer` 持仓归属标记，出场规则按**层级归属**过滤（T 路径不登记层级） |
| D17 | L2 整层被换手预算锁死（46% 交易日暂停、[L2-买] 全期 0 笔） | `ytd_turns` 只增不减 + 年化外推，L3 日内T 几十笔回合 30 日内烧穿年度预算 | L2/L3 **分账** + L2 改用 **250 日滚动窗口**，窗口外旧回合自动脱落、预算可恢复；T 换手由 `T_MAX_ROUNDS`/日独立封顶 |
| D18 | L3 日内T 绕过统一出口（24 笔 T 未入统计、拒单不熔断、幽灵状态复活） | `_t_once`/`_t_force_close` 直连 `order_target_value` | 全部改走 `_submit_order` 统一出口（含拒单/熔断/统计一致处理）；开新T 加断路器感知 |
| D19 | 统计「已提交N」是记账次数而非真实委托数 | `STATS` 计数与平台实际下单混用 | 新增 `order_submitted`/`order_rejected` 真实计数，统计行改为「实际下单/被拒/有效」 |
| D20 | 首日 74 条 `回测不支持get_snapshot` 噪音 | `_cur_price` 回退调用 `get_snapshot`，回测模式平台刷告警 | 移除 `get_snapshot` 回退（handle_data 的 `data` 已是权威盘中价） |
| D21 | 同标的同日「先砍两成再清仓」矛盾委托 | 盘前多条风控路径对同一 code 各发一笔卖单 | `_flush_pending` 同标的合并（取末笔绝对目标市值覆盖前笔） |

### 离线验证器（verify_v12.py，无平台环境运行）
双端（山西/国金）各 **16/16 断言通过**：
- D16a L1 核心仓不被 L2 止盈清掉 / D16b L2 卫星仓满足止盈被卖出
- D17a L3 单独记账且高频不锁 L2 / D17b 超预算暂停 + 窗口外恢复
- D18 T 经统一出口下单 + 目标市值正确 + 拒单不污染状态
- D19 真实委托/拒单计数准确
- D20 不调用 get_snapshot
- D21 同标的委托合并、末笔覆盖
- V1.2 指纹存在

### 重跑必须核对的 8 项（山西平台，分钟级周期）
1. 初始化日志指纹 `★山西PTrade V1.2★`（国金为 `★国金PTrade V1.2★`）
2. `[交易执行] 受理N 被拒0`，且 `get_snapshot` 告警 = 0 条
3. L1 首日建仓约 6~9 只、仓位 ≈58%（资金再分配生效）
4. `[L2-卖]` 只针对**真正 L2 建仓**的标的；L1 核心仓不再被日内波动清掉
5. `[L2-买]` 不再全期为 0（预算不再被 L3 拖穿）；L2 波段可正常建仓
6. 统计行出现 `L2回合 / L3回合` 分计；`实际下单/被平台拒/有效` 三者闭合
7. 日内T 委托全部出现在 `[成交受理]`/`[委托被拒]` 统计内（不再隐身）
8. 收益应显著高于 V1.1 的 +3.49%（仓位建得起来 + L2 不再空转）


## 九、V1.3 修复与验证（2026-09-22）

### 背景：V1.2 收益 -0.04%，"收益更差了"
V1.2 真下单回测：300000 → 299866（-0.04%），峰值 +4.0%（01-30 见顶），最大回撤 -6.84%。
归因发现：

- **V1.1 的 +3.49% 是被 D16 缺陷撑起来的**——L2 的 +14% 止盈规则被错用到 L1 持仓，
  等于"涨 14% 就落袋"，恰好吃到 1-4 月上涨、躲过后半段下跌。V1.2 修掉缺陷后 L1 纯长持，
  收益回落到 -0.04%（核心=L1 beta + 市场开关低位砍仓拖累、L2 死、L3 打滑）。
- 三层分工实测：L1 被"市场开关L1减仓"反复砍（持仓占比 53.5%→33%）；L2 因沪深300 在60日线
  下方被暂停 68 次、仅恢复 12 次，整段 0 回合；L3 跑 56 回合但综合成本率仅 0.2%，净贡献≈0。

### 修复方向（用户拍板：L1 止盈再平衡）
把 V1.1 那个 bug 干的事变成**正式纪律**：L1 单票浮盈 ≥ `L1_TRIM_GAIN`(0.15) 且市值超目标权重
时，减仓至目标权重（保留长线底仓、不空仓），释放现金；现金由季度 `_l1_rebalance` 自动回补到
欠配的 L1 标的（买弱），形成"卖强买弱"的波动率收割闭环。

### V1.3 变更清单（双端同步，`py_compile` 通过）
1. 新增常量 `L1_TRIM_GAIN=0.15`、`L1_DRIFT_CTRL_DAYS=10`
2. `initialize` 新增 `g["last_drift_day"]`
3. `before_trading_start` 每 10 个交易日调用 `_l1_drift_control(total)`
4. 新增函数 `_l1_drift_control(total)`：浮盈达标且超配→减仓至目标权重
5. 全局版本升版 V1.2 → V1.3（43 处）

### 离线验证器（verify_v13.py）
- 山西 / 国金 **9/9 全过**：浮盈达标且超配→减仓至目标；浮盈未达标/未超配/L2层/不足最短持有
  /小仓未超配→均不减仓；V1.3 指纹存在。
- **回归**：verify_v12（D16–D21）双端 15 项行为断言全过，无回归（唯一"FAIL"是 V1.2 指纹检查，
  因代码已升 V1.3，属预期）。

### 重跑必须核对的 7 项（山西平台，分钟级周期，真下单）
1. 指纹 `★V1.3★`
2. `[L1-止盈]` 日志出现（浮盈≥15% 的标的被减仓至目标权重）
3. 季度再平衡日志 `[L1] 季度再平衡启动` 后，欠配标的被回补（买弱）
4. 总收益应显著高于 V1.2 的 -0.04%，且峰值后不再单纯回吐（落袋生效）
5. `订单不在交易时间段内` / `股票委托数量为0` 仍为 0
6. 市场开关 L2 暂停/恢复次数、L2 回合数（若仍 0，见 §八/§九结构性问题）
7. 不再有 `get_snapshot` 噪音、同标的矛盾委托


---

## 十、V1.4 修复与验证（2026-09-22）

> 触发：V1.3（30 万本金 / 分钟级 / 真下单）回测结果仅 **+2.01%**，实测病根是
> **仓位单向失血**（砍 6 次、只补 1 次）+ **L2 整层空转**（0 回合）。

### 10.1 本轮修复（D22~D29）

| 编号 | 缺陷 | 实测证据（V1.3 日志原文） | V1.4 改法 |
|---|---|---|---|
| **D22【P0】** | 市场开关「砍两成」**单向失血** | 全期砍 **6 次**（03-05 / 03-10 / 03-20 / 07-14 / 07-17 / 08-19），而「档位复位」5 次**只清标记、不回补仓位** → 持仓占比 53.5% 一路砸到 32.0%，现金趴到 67% | 档位复位时执行 `_l1_refill`，**真的把仓位补回来** |
| **D23【P0】** | 层额度按「标的是否在池里」统计，把 L1 底仓算成 L2 已用额度 | 03-13 L2 出信号时「可用额度 21951」，而 21951 = 75000 − 53049，**53049 元全是 L1 建的仓** → L2 名义 25% 被抽干，全期 0 回合 | `_layer_room(..., layer)` 按 **l2_qty 份额**统计 |
| **D24【P0】** | L2 池全是买不起的科创板高价股 | 5 次信号全落 688072（1手 6.6 万）/ 688012（5.5 万）/ 688111（4.6 万）；L2 等权单票预算仅 7500 元 | L2 池换为 **8 只 1 手 1.2千~9千** 的中低价股 |
| **D25** | 季度再平衡是唯一回补窗口，错过即断档 60 天 | 07-07 季度再平衡整轮失败：`[资金再分配-L1] 全部标的均买不起1手` | 新增 `_l1_refill` **三触发点**：市场开关复位 / 止盈后立即 / 每 5 日兜底 |
| **D26** | 同一标的无法 L1/L2 分层持有 | `owner_layer` 单值 → L2 建仓会抢走 L1 的层级标记 | 新增 `l2_qty / l2_entry_px / l2_buy_day` **份额记账**，出场只卖自己那份 |
| **D27** | L3 日内T 净贡献≈0 却贡献大部分摩擦 | V1.3 实测 T 综合成本率仅 0.2%、净贡献≈0 | `INTRADAY_T=False` 移除；`_intraday_hard_stop` 与 L3 开关**解耦**保留 |
| **D28** | L1 单票吃满 10% 挤死 L2 | 自检原文：L1 再分配后单只 6.44%，L2 仅余 3.56%/只 x 4 只 = 14.2%（名义 25%） | `_l1_single_cap = L1_RATIO / L1可买数`，主动给 L2 让出单票空间 |
| **D29** | L2 信号三重条件叠加过严 | 全期仅 5 次信号 | `L2_PULLBACK_PCT` 6%→5%、量比 0.8→0.9、RSI 上限 55→58；新增「接近信号」计数诊断 |

### 10.2 验证结果（离线校验器，双端同步）

| 校验器 | 山西 | 国金 |
|---|---|---|
| **verify_v14.py（V1.4 新增，34 项）** | **34/34** | **34/34** |
| verify_v13.py（V1.3，9 项）回归 | 9/9 | 9/9 |
| verify_v12.py（V1.2，16 项）回归 | 16/16 | 16/16 |

- 双端 `py_compile` 通过；两版差异 **21 行**，全为券商名/文档文本，**交易逻辑零差异**。
- V1.4 校验覆盖：复位即回补 / 止盈后立即回补 / 缺口门槛 / 同标的冷却 / 层额度按份额 /
  L2 池换池 / L1-L2 同标的分层（只卖自己份额）/ L3 移除后日内止损仍生效 /
  L1 单票收敛 / L1 已持仓不影响 L2 独立建仓 / L2 份额记账正确。

### 10.3 重跑验证清单（山西平台，**必须分钟级 + 真下单模式**）

| # | 检查项 | 预期 |
|---|---|---|
| 1 | 版本指纹 | `★山西PTrade V1.4★`；且出现 `[初始化] ★V1.4 变更★` |
| 2 | 资本适配自检 | `L1 单票上限收敛为 X.XX%（= L1_RATIO/可买N只）`，且 L2 单票空间 ≥ 名义 25% 的 60% |
| 3 | **回补链路（核心）** | `[统计] ★V1.4 回补链路：累计回补N只`，**N > 0**（V1.3 为 0） |
| 4 | **市场开关双向（核心）** | 复位日志变为「→ L1 档位复位，**执行回补**」，且随后出现 `[L1-回补] 市场开关L1回补` |
| 5 | L1 止盈后立即回补 | `[L1-止盈] ... 立即回补欠配标的（★V1.4 不再等季度）` |
| 6 | **仓位落实率（核心验收）** | 平均持仓占比应显著高于 V1.3 的 **41.1%**，目标 **60%+** |
| 7 | **L2 有成交（核心）** | 出现 `[L2-建仓] ... L2份额+N股`；`[统计] L2回合 > 0`（V1.3 为 0） |
| 8 | L3 已移除 | 无 `T开仓 / T回补 / T强平`；统计行含 `L3=已移除` |
| 9 | L1/L2 分层 | L2 卖出日志含「（L2份额N股，**L1底仓保留**）」 |
| 10 | 无回归 | `实际下单N 被平台拒0`；无 `get_snapshot` 噪音 |
| 11 | **收益** | 应显著高于 V1.3 的 **+2.01%**。若无改善 → 说明病根不在仓位而在选股/择时，需转策略设计层（见 10.4） |

### 10.4 若 V1.4 仍不改善，下一步的诊断方向

V1.4 只解决「**结构性失血**」。以下三条属**策略设计层**，本轮未动，需回测数据后决策：

1. **市场开关的择时有效性** —— 跌破 120 日线砍两成的规则，在震荡市可能仍是负 alpha（V1.3 的 6 次砍仓是否真的避开了下跌，需逐次核对砍后 10 日指数涨跌）。
2. **L2 信号的市场开关过严** —— 60 日线下即暂停 L2（V1.3 实测 85% 交易日被关），是否放宽到仅熔断级才关。
3. **选股池与本金匹配** —— 16 只精选池里仅 9 只买得起（茅台 1 手 13 万），若本金不放大，选股池需压缩或替换。

### 10.5 回滚

同目录 `山西PTrade_三层量化策略_V1.3_backup.py`（另有 `_V1.2_backup.py` / `_V1.0_backup.py`）。
