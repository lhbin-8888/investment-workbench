# -*- coding: utf-8 -*-
# =============================================================================
# 热点融合 v1.8  (PTrade / Python 3.5 兼容)
# 融合对象：热点早盘001 V4.11 / 热点追踪lhb888 v3 / 热点追踪早盘1030 v8.1
# 设计目标：取三家之长、避三家之短 -> 更高收益 + 各市场环境下更平滑
#
# [指纹] ★FUSION v1.8★
#   版本演进 = v1.0(首次真机跑，字段名踩坑全天0成交) -> v1.1(字段白名单+当日价兜底)
#              -> v1.1.1(补逐日[净值]快照 + 空转看门狗 + 取数失败升格error)
#              -> v1.2(分批取数 + 当日价三层兜底 + 001假设定价兜底)
#              -> v1.3(照抄001已验证的取数形态 is_dict=True + fq降级；结构化数组按
#                      dtype.names 解析；日线是否含当日改用 date 列运行时判定)
#              -> v1.4(当日价改为「数值优先 + 每层拒绝==昨收」的四层兜底；date 列
#                      别名补 datetime；接入外挂 sector_map.json 修行业恒空；
#                      量比退化到昨日口径；新增 [粗筛] 漏斗 + [口径] 样本自证行)
#              -> v1.5(净值自算持仓市值修 HALT日计价bug；止损ATR倍1.8/下限6%；赢家
#                      粘性门槛12%+回撤10pp锁利，激活时豁免移动止盈/破均线让利润多跑)
#              -> v1.6(大市闸门分层：双弱(中证1000&沪深300 同时破MA20)才 HALT 全停，
#                      单弱降为 HALF 半仓；新增 [大市] 自证行(两指数close/MA20/偏离)；
#                      修 report_stats「空行业名」误报 —— 改为按占比判定；v1.5 只要
#                      有 1 只未命中就报 error，把「个别新股缺行业」误判成「整层失效」)
#              -> v1.6.1(纯观测补强，不改交易行为：新增 [精选] 六道闸漏斗行，把「粗筛
#                      过关却买不到」的原因打出来；buy_job 三处静默 return 出声
#                      [候选为0 / 持仓已满 / 账户异常])
#              -> v1.7(行业表内嵌：把 416KB 外挂 sector_map.json 反向组织成
#                      「板块名:代码 代码|...」压缩串(板块名只存一次，416KB->95KB)
#                      直接写进策略，单文件上传、零路径依赖、零上传遗漏；
#                      外挂多路径逻辑原样保留作双保险；表由 gen_embedded_map.py 生成)
#              -> v1.8(行业层口径根因修复：行业均值改用【全市场扫描池】而非候选池
#                      (候选池已被涨幅区间截断 -> 均值全挤同区间 -> 排序接近随机，
#                       实测行业榜淘汰占全部淘汰 87.2%、候选中位数仅 2 只)；
#                      加 SECTOR_MIN_STOCKS=5 过滤小样本行业；行业层失效则降级为
#                      「不做行业过滤」不误杀；行业表切【申万一级 31 行业】
#                      (归属唯一、与 lhb888 同口径)；入场加 MA20 缓冲 2% +
#                      涨幅区间对齐 lhb888(主板3~4%/创科6~8%) 降追高)
#   热点判定 = 申万一级行业均值(全市场样本,↑) + 板块强度分(001) + 共振>=2(常开, 改1030默认OFF)
#   选股     = 涨幅主板[3%,4%]/创科[6%,8%](v1.8对齐lhb888) + 量比>=1.5 + 站MA20(+2%缓冲)&MA60 + 形态>=2 + 日内回撤<=4% + 回踩cur>=open
#   买入     = 10:30 + 每日新<=2 + 冷静期2日 + 双护栏 + 强度加权 + 额度回收3轮
#   止损     = ATR自适应 clamp[6%,11%](倍1.8) + 硬止损下限09:45
#   止盈     = +10%减半 + 保本+3% + 移动止盈武装5%/回撤15%(赢家豁免) + 赢家粘性锁利12%+10pp
#   破线     = 破10日线(14:45确认,浮亏才砍) + 破20日线(14:45确认,硬清)
#   大市     = 双弱(1000&300 同破MA20)→HALT全停 + 单弱→HALF半仓 + 情绪冰点HALF减半(v1.6分层)
# =============================================================================

import json
import math
# 注意：PTrade 3.5 环境禁用 os / 网络 / f-string / 类型注解

# ----------------------------- 平台字段名容错层 ------------------------------
# 【2026-09-22 实盘回测踩坑】平台 get_history 只认下面这 12 个字段名：
#   open high low close price volume money preclose high_limit low_limit
#   unlimited is_open
# 昨收的字段名是 preclose（无下划线）；写成 pre_close 会被平台直接拒绝并抛
# invalid field 异常。该异常若被上层 try/except 吞掉，表现为「候选池恒为空、
# 全天 0 成交、净值一条直线」——最隐蔽也最致命的一类故障。
PLATFORM_FIELDS = ("open", "high", "low", "close", "price", "volume", "money",
                   "preclose", "high_limit", "low_limit", "unlimited", "is_open")
FIELD_ALIAS = {
    "pre_close": "preclose", "preClose": "preclose", "prev_close": "preclose",
    "last_price": "price", "current_price": "price", "avg_price": "price",
    "vol": "volume", "amount": "money", "turnover": "money",
}


def _norm_fields(fields):
    """逻辑字段名 -> 平台合法字段名。未识别的一律剔除（宁缺毋滥，绝不触发 invalid field）。"""
    if isinstance(fields, str):
        fields = [fields]
    out = []
    for f in fields:
        k = FIELD_ALIAS.get(f, f)
        if k in PLATFORM_FIELDS and k not in out:
            out.append(k)
    return out


def _sub_rows(sub, fields):
    """单只返回值 -> (rows, idxmap)。rows=[[...],...] 每行一个交易日；idxmap={字段名: 列下标}。

    一律【优先按列名】对齐（平台 DataFrame 的列名即请求字段名），取不到列名时才
    退回按请求顺序的位置映射。理由：依赖位置取数会在「字段被平台剔除/顺序变化」时
    静默错列——不报错，但会一直选出错的票。
    """
    if sub is None:
        return None, None
    cols = None
    try:
        c = getattr(sub, "columns", None)
        if c is None and isinstance(sub, dict):
            c = list(sub.keys())
        if c is not None:
            cols = [str(x) for x in list(c)]
    except Exception:
        cols = None

    rows = None
    # ① {字段: [序列]} 形态（部分版本逐字段返回）
    if isinstance(sub, dict) and cols:
        n = 0
        for k in cols:
            v = sub.get(k)
            if isinstance(v, (list, tuple)):
                n = max(n, len(v))
        if n > 0:
            rows = []
            for i in range(n):
                row = []
                for k in cols:
                    v = sub.get(k)
                    if isinstance(v, (list, tuple)) and i < len(v):
                        row.append(v[i])
                    else:
                        row.append(None)
                rows.append(row)
    # ② numpy 结构化数组 —— 本平台 get_history(is_dict=True) 每值的【真实形态】
    #    实测 dtype.names = ('date','open','close','high','low','volume')：
    #      · 含 date 列，且列顺序不保证等于请求顺序 ⇒ 只能【按字段名】取列；
    #      · 旧实现会落进下面的 list(r) 分支：结构化标量不可迭代 → 每行退化成
    #        [记录对象] → 所有列都取不到 → 有效票恒 0，而且不报任何错。
    if rows is None and getattr(sub, "dtype", None) is not None:
        names = getattr(sub.dtype, "names", None)
        if names:
            try:
                rows = []
                for rec in sub:
                    rows.append([rec[k] for k in names])
                cols = [str(k).lower() for k in names]
            except Exception:
                rows = None
        else:
            # 无字段名的普通 2D 数组：按 001 实测口径 (date, open, close, high, low, volume)
            try:
                rows = [list(r) for r in sub]
                cols = ["date", "open", "close", "high", "low", "volume"][:len(rows[0])] if rows else None
            except Exception:
                rows = None
    # ③ DataFrame / list[list] 形态
    if rows is None:
        if isinstance(sub, dict):
            return None, None
        src = getattr(sub, "values", None)
        if src is None:
            src = sub
        try:
            rows = []
            for r in src:
                try:
                    rows.append(list(r))
                except Exception:
                    rows.append([r])
        except Exception:
            return None, None
    idx = {}
    if cols:
        for i, c in enumerate(cols):
            if c not in idx:
                idx[c] = i
        if len(cols) == len(fields):
            for i, f in enumerate(fields):
                idx.setdefault(f, i)
    else:
        for i, f in enumerate(fields):
            idx[f] = i
    return rows, idx


def _col(rows, idxmap, name):
    """按列名取整列 -> list[float]；整列有任一值不可转则返回 None（宁可弃票，不可错位）。"""
    if not rows or not idxmap or name not in idxmap:
        return None
    j = idxmap[name]
    out = []
    for r in rows:
        try:
            out.append(float(r[j]))
        except Exception:
            return None
    return out


def _last_val(rows, idxmap, name):
    """取该列最后一个有效值（昨收等只需末值的场景，容忍前段缺失）。"""
    if not rows or not idxmap or name not in idxmap:
        return None
    j = idxmap[name]
    v = None
    for r in rows:
        try:
            v = float(r[j])
        except Exception:
            continue
    return v


def _col_last(rows, idxmap, name):
    """末值取数（语义同 _last_val，命名对齐 _col 便于阅读）。"""
    return _last_val(rows, idxmap, name)


def _col_first(rows, idxmap, names):
    """按【别名列表】取第一个存在的整列 —— 平台同一语义的列名不唯一。

    【v1.4 修正】本平台结构化数组里日期列叫 `datetime`，不叫 `date`（见 [诊断]
    的"解析列=[...'datetime'...]"）。旧代码硬查 "date" 恒取空 → has_today 恒 False
    → 口径判定失效（并且日志照样写"不含当日"，属假判定，把排查带偏）。
    """
    if not rows or not idxmap:
        return None
    for nm in names:
        if nm in idxmap:
            return _col(rows, idxmap, nm)
    return None


# ===================== 平台取数协议层（v1.3 根因修复） ========================
# 【2026-09-22 第三轮真机回测：分批=27 批，合并后键数仍=0】
#   上一轮把「整池一次请求」改成分批 200 只，仍然全空 —— 说明问题不在批大小。
#   决定性证据来自 001 策略（同一平台、同一接口、真能取到 5183/5645 只）的日志：
#       [数据层] get_history 多证券返回形态 = dict （批量可用，全池一次取）
#       [诊断] get_history 返回 type=OrderedDict mode=dict
#       [诊断] dict 共 200 键     <-- 一批正好 FETCH_CHUNK 只
#       [诊断] 首值 type=ndarray len=70 repr=array([(20260114, 32.14, ...)])
#   结论：本券商必须显式 [is_dict=True] 才返回 {代码: 结构化数组}；不传时返回的
#   形态策略侧不认识，而被旧 _fetch_panel 静默丢弃 —— 于是日志表现为「面板为空
#   dict」，把一个"形态不匹配"误报成"没有数据"，白查了两轮。
#   001 在 2026-09-14 踩过同一族的坑（见其源码 V4.1 数据层补丁：fq='pre' 返回空而
#   不抛异常）。这里照抄其已被 38 个交易日、5183/5645 只验证过的做法：
#     ① 显式传 is_dict=True；② 空结果一律当失败；③ 逐级降级且必须显式告警。
_DS = {"style": None, "style_name": "未探测", "diag": False}

# 候选调用形态，按「001 实测有效」程度排序；探测命中即缓存复用，不再反复试错。
_HIST_STYLES = (
    ("dict+fq", {"fq": "pre", "is_dict": True}),
    ("dict",    {"is_dict": True}),
    ("fq",      {"fq": "pre"}),
    ("plain",   {}),
)


def _has_data(r):
    """返回结果里是否真的含 K 线 —— 空结果（含空 dict）一律视为【失败】。

    ★这是 001 V4.1 的核心修正★：本券商 get_history 在参数不被支持时
    「返回空结果而不抛异常」。若把"无异常"当成"成功"，就会永久锁死在空通道上：
    每批皆空、零成交，且日志一声不吭。
    """
    if r is None:
        return False
    if isinstance(r, dict):
        for v in r.values():
            try:
                rows, _idx = _sub_rows(v, ("close",))
                if rows:
                    return True
            except Exception:
                continue
        return False
    try:
        if getattr(r, "empty", None) is not None:
            return not bool(r.empty)
        return len(r) > 0
    except Exception:
        return bool(r)


def _hist_style(codes):
    """探测并缓存「本平台认」的取数参数形态；只在首次用到时探一次。"""
    if _DS["style"] is not None:
        return _DS["style"]
    probe = list(codes[:2]) if codes else []
    if not probe:
        probe = ["000001.SZ", "600000.SS"]
    for name, kw in _HIST_STYLES:
        try:
            r = get_history(3, "1d", ["close"], probe, **kw)
        except Exception as e:
            log.error("[降级] 取数形态 %s 参数不被支持（%s）→ 试下一档"
                      % (name, repr(e)[:90]))
            continue
        if _has_data(r):
            _DS["style"] = kw
            _DS["style_name"] = name
            log.info("[取数] 形态探测命中 = %s（本次回测所有取数复用此形态）" % name)
            return kw
        log.error("[降级] 取数形态 %s 返回空结果（无异常）→ 视为不可用，试下一档" % name)
    log.error("[取数] 四档形态全部取不到数据 → 平台接口/代码后缀/字段名三者之一有问题，"
              "已退回 plain 形态继续跑（后续报错落在真实业务路径上）")
    _DS["style"] = {}
    _DS["style_name"] = "plain(兜底)"
    return {}


def _hist_call(n, freq, fields, codes):
    """统一取数入口：形态探测 + 空结果判定 + 逐级降级。全策略只此一处直调平台。"""
    if not _has("get_history"):
        return None
    kw = _hist_style(codes)
    try:
        return get_history(n, freq, list(fields), list(codes), **kw)
    except Exception as e:
        _DS["style"] = None      # 缓存形态偶发失效 → 清缓存，下轮重新探测
        log.error("[取数] %s 形态调用失败，已清缓存待重探: %s"
                  % (_DS.get("style_name"), repr(e)[:110]))
        return None


# ----------------------------- 全局参数区 -----------------------------------
SIGNAL_TIME      = "10:30"          # 选股+买入主时点
RISK_TIMES       = ["09:45", "11:20", "13:10", "14:15", "14:45", "14:55"]  # 盘中巡检
TAIL_RISK_TIMES  = ["14:45", "14:55"]   # 结构时点（尾盘确认类）
BREAK_MA_TIME    = "14:45"          # 破均线仅在此时点后确认
NET_VALUE_TIME   = "14:55"          # 逐日净值快照时点（缺此行→回测后拿不到同口径净值曲线）
IDLE_WARN_DAYS   = 5                # 连续空仓 N 日触发 [空转告警]（防"静默不交易"整段回测作废）

# --- 热点/板块 ---
TOP_MAIN_LINES   = 3                # 取前 N 主线（001）
SECTOR_FADE_RATIO = 0.5             # 衰退：涨停率<=前5日均*0.5
SECTOR_FADE_DAYS  = 2               # 连续确认天数（001 修复点）
MIN_SECTOR_ALIGN = 2                # 同行业>=2只才共振（常开，改1030 OFF）
INDUSTRY_MIN_RISE = 0.0             # 申万行业均值>0才列为强势（lhb888）
TOP_INDUSTRY_N  = 10               # 申万行业均值排名前 N（lhb888）
SECTOR_MIN_STOCKS = 5              # v1.8 行业内当日样本>=N只才参与排名（lhb888 同款；过滤小样本行业噪声）
SECTOR_CONFIRM_FALLBACK = True     # v1.8 行业层失效(表空/无有效样本) -> 降级为「不做行业过滤」，不误杀候选
MA20_ENTRY_BUFFER = 0.02           # v1.8 入场 MA20 缓冲：须站上 MA20 至少 2%（lhb888 同款，避免贴线买入）

# --- 选股 ---
MIN_HIST_BARS    = 60
MAX_HIST_BARS    = 60               # 面板取数根数（与 MIN_HIST_BARS 同源，勿改名）
# 单次 get_history 的证券数上限。001 日志 [诊断] 明写「dict 共 200 键」（请求 5645
# 只）→ 平台会静默截断批量请求，必须自己分块取数再合并。
FETCH_BATCH      = 200
# ★v1.4 面板字段集★ 在 v1.3 的 5 字段上补回 price / money —— 两者都在平台白名单内，
# 且 1030 策略在同平台的回测里【长期稳定请求它们】(close,price,volume + money 分批)：
#   · price 字段 = 日线末根的「收盘价」快照，是当日价三层兜底的第 2 档输入；
#   · money 字段 = 成交额，直接用平台值比自算（volume×close）少一个未知变量。
PANEL_FIELDS     = ["open", "close", "high", "low", "volume", "price", "money"]
# 日期列别名：本平台实测列名是 datetime（不是 date）。列名一变，口径判定就静默失效。
DATE_COL_ALIASES = ("date", "datetime", "time", "trade_date", "dt")
# 行业归属表：v1.7 起【内嵌优先】（见同区 EMB_SECTOR_RAW，由 gen_embedded_map.py
# 自动生成），下列外挂文件 + 候选路径仅在「内嵌为空」时作为兜底启用（双保险）。
# 表缺失则"行业均值+共振"整层失效，表现为 [统计] 里板块强度 Top 全是空字符串 ''。
SECTOR_MAP_FILE  = "sector_map.json"
SECTOR_MAP_PATHS = (
    "sector_map.json",
    "只读/sector_map.json",
    "../只读/sector_map.json",
    "readonly/sector_map.json",
    "user/19264938/files/sector_map.json",
    "/user/19264938/files/sector_map.json",
)
# === EMB_IND_BEGIN ===
# source: industry_map.json (flat)
# 原表: 123.6 KB JSON | 内嵌: 36.0 KB | 板块 31 / 标的 5218
# 格式: "板块名:代码 代码 ...|板块名:..."  代码为 6 位
# 生成: python gen_embedded_map.py --src D:\投研工作台\06-投资策略分析\策略回测\热点追踪lhb888\industry_map.json --kind flat
EMB_SECTOR_RAW = (
    "农林牧渔:000019 000048 000505 000592 000663 000702 000713 000735 000798 000876 000911 000930 000972 000998 001201 001313 001338 001366 002041 002069 002086 002100 002124 002157 002215 002234 002286 002299 002311 002321 002385 002458 002481 002548 002556 002567 002679 002688 002696 002714 002746 002772 002852 002868 002891 002982 003030 300021 300087 300094 300119 300138 300175 300189 300268 300313 300498 300511 300673 300761 300871 300967 300970 300999 301116 301498 600097 600108 600127 600191 600195 600201 600251 600257 600265 600313 600354 600359 600371 600467 600540 600598 600737 600883 600962 600965 600975 601118 601952 603151 603182 603231 603336 603363 603477 603566 603609 603668 603717 605198 605296 688098 688156 688526"
    "|基础化工:000408 000420 000422 000510 000525 000545 000553 000565 000635 000677 000683 000691 000707 000731 000782 000792 000818 000822 000830 000859 000881 000893 000902 000912 000920 000936 000949 000973 000990 001207 001217 001218 001231 001255 001296 001325 001333 001335 001358 001359 001369 001378 002001 002037 002054 002064 002068 002092 002096 002108 002109 002136 002145 002165 002166 002170 002206 002211 002224 002226 002246 002250 002254 002258 002263 002274 002312 002319 002324 002326 002360 002361 002381 002386 002391 002395 002407 002427 002430 002440 002442 002453 002455 002470 002496 002497 002513 002522 002538 002539 002545 002549 002562 002585 002588 002591 002597 002601 002632 002637 002648 002666 002669 002683 002734 002748 002749 002753 002758 002768 002783 002802 002809 002810 002825 002827 002838 002886 002895 002909 002915 002917 002942 002971 002998 002999 003002 003017 003042 300019 300041 300067 300072 300082 300107 300109 300121 300163 300169 300174 300180 300200 300214 300218 300221 300225 300230 300243 300261 300305 300320 300321 300343 300387 300405 300478 300487 300505 300522 300535 300538 300539 300575 300586 300587 300596 300610 300637 300641 300644 300665 300717 300721 300727 300731 300741 300758 300767 300796 300798 300801 300804 300806 300821 300829 300834 300848 300858 300876 300886 300891 300905 300910 300920 300927 300980 300995 301003 301019 301035 301036 301037 301057 301059 301069 301071 301076 301077 301090 301092 301100 301118 301131 301149 301156 301190 301196 301206 301209 301212 301216 301220 301237 301256 301283 301286 301300 301373 301395 301459 301518 301538 301555 301565 301585 301591 301617 301618 301665 301683 301687 301688 600063 600075 600078 600096 600135 600141 600143 600160 600165 600227 600230 600273 600299 600309 600319 600328 600331 600352 600367 600370 600378 600389 600409 600423 600426 600470 600486 600500 600596 600610 600618 600623 600691 600714 600722 600727 600731 600746 600784 600810 600844 600866 600873 600929 600935 600955 600989 601026 601065 601113 601208 601216 601568 601678 603004 603010 603020 603033 603041 603051 603062 603065 603067 603073 603077 603079 603086 603110 603113 603120 603125 603150 603155 603172 603181 603188 603192 603213 603217 603225 603227 603235 603255 603260 603276 603281 603285 603299 603310 603330 603332 603334 603360 603379 603382 603395 603406 603435 603505 603580 603585 603599 603612 603639 603650 603663 603681 603683 603688 603722 603739 603790 603810 603822 603823 603826 603867 603879 603916 603928 603938 603948 603968 603970 603977 603980 605008 605016 605020 605033 605077 605086 605166 605183 605366 605399 605488 605566 605589 688065 688089 688129 688133 688157 688179 688196 688199 688203 688219 688267 688269 688295 688300 688323 688350 688357 688386 688398 688571 688585 688598 688602 688625 688639 688659 688669 688716 688718 688722"
    "|钢铁:000629 000655 000708 000709 000717 000761 000778 000825 000898 000923 000932 000959 001203 002075 002110 002318 002443 002478 300881 600010 600019 600022 600117 600126 600231 600282 600295 600307 600382 600399 600507 600516 600569 600581 600782 600808 601003 601005 601121 601123 601686 601969 603878 603995"
    "|有色金属:000060 000426 000506 000603 000612 000630 000633 000657 000688 000737 000751 000758 000762 000795 000807 000831 000878 000933 000960 000962 000969 000970 000975 001257 001280 001337 002114 002149 002155 002160 002167 002171 002182 002192 002203 002237 002240 002295 002378 002379 002428 002460 002466 002501 002532 002540 002578 002716 002738 002756 002824 002842 002978 002988 002996 003038 300057 300127 300139 300224 300328 300337 300618 300697 300748 300811 300835 300855 300930 300963 301026 301141 301219 301323 301522 301531 301622 600111 600219 600255 600259 600281 600301 600338 600361 600362 600366 600392 600456 600459 600489 600490 600497 600531 600547 600549 600595 600615 600711 600768 600888 600961 600988 601020 601069 601137 601168 601212 601388 601600 601609 601677 601702 601899 601958 603045 603072 603124 603132 603271 603399 603407 603527 603799 603876 603937 603978 603979 603993 605158 605208 605376 688077 688102 688190 688231 688456 688750 688786 688811"
    "|电子:000020 000021 000045 000050 000062 000100 000509 000536 000636 000670 000725 000727 000823 001232 001287 001298 001308 001309 001314 001373 001389 001399 002036 002045 002049 002055 002077 002106 002119 002130 002134 002137 002138 002139 002141 002156 002161 002185 002199 002213 002217 002222 002241 002273 002289 002351 002369 002371 002384 002387 002388 002402 002409 002426 002436 002449 002456 002463 002475 002484 002552 002579 002584 002587 002600 002635 002636 002643 002654 002655 002660 002681 002729 002741 002745 002782 002815 002841 002845 002855 002859 002861 002866 002876 002885 002888 002913 002916 002922 002925 002938 002947 002952 002955 002962 002981 002983 002992 002993 003015 003019 003026 003043 300032 300046 300053 300054 300076 300077 300088 300102 300115 300120 300128 300131 300136 300162 300184 300219 300223 300232 300236 300241 300256 300269 300285 300296 300301 300303 300319 300322 300323 300327 300331 300346 300373 300389 300398 300408 300429 300433 300456 300458 300460 300475 300476 300481 300489 300493 300516 300537 300545 300566 300576 300582 300602 300604 300613 300623 300632 300647 300650 300655 300656 300657 300661 300666 300671 300672 300679 300684 300686 300701 300706 300708 300709 300735 300736 300739 300752 300782 300784 300787 300793 300808 300812 300814 300822 300831 300843 300852 300857 300866 300868 300889 300903 300909 300916 300936 300939 300940 300951 300956 300964 300968 300975 300976 300991 301031 301041 301045 301051 301067 301086 301099 301106 301123 301132 301176 301180 301183 301189 301251 301275 301282 301285 301297 301308 301319 301321 301326 301328 301329 301348 301359 301366 301369 301379 301383 301387 301389 301421 301458 301479 301486 301489 301491 301536 301563 301566 301567 301583 301606 301611 301626 301628 301629 301630 301666 301678 301717 600071 600130 600171 600183 600203 600206 600237 600330 600353 600360 600363 600460 600552 600563 600584 600601 600641 600703 600707 600745 600877 601138 601231 603002 603005 603052 603061 603068 603078 603115 603160 603175 603186 603228 603290 603296 603297 603316 603327 603328 603341 603375 603380 603386 603459 603501 603595 603626 603629 603633 603679 603685 603690 603703 603738 603773 603890 603893 603920 603931 603933 603936 603986 603989 603991 605058 605111 605218 605258 605277 605358 605588 688002 688007 688008 688010 688012 688018 688019 688020 688035 688036 688037 688041 688045 688047 688048 688049 688052 688055 688061 688072 688079 688082 688093 688099 688103 688106 688107 688110 688120 688123 688126 688127 688130 688135 688138 688141 688146 688150 688153 688167 688172 688173 688181 688183 688195 688200 688209 688210 688213 688216 688220 688230 688233 688234 688249 688252 688256 688259 688260 688261 688262 688268 688270 688279 688286 688299 688307 688322 688325 688332 688347 688352 688359 688361 688362 688368 688371 688372 688378 688380 688381 688385 688391 688396 688401 688403 688409 688416 688419 688432 688449 688458 688469 688478 688484 688486 688496 688498 688502 688508 688512 688515 688519 688521 688525 688530 688535 688536 688538 688539 688545 688548 688549 688550 688582 688584 688589 688591 688593 688595 688601 688603 688605 688608 688620 688652 688653 688655 688661 688662 688678 688683 688689 688691 688693 688699 688702 688709 688711 688720 688721 688727 688728 688729 688766 688775 688781 688783 688785 688790 688795 688797 688798 688800 688801 688802 688807 688809 688820 688825 688981"
    "|家用电器:000016 000333 000404 000521 000541 000651 000801 000810 000921 001259 001268 001326 001387 002005 002011 002032 002035 002050 002076 002242 002290 002403 002418 002429 002508 002543 002614 002638 002668 002676 002677 002705 002723 002724 002848 002860 002959 003023 003028 300160 300217 300247 300272 300279 300342 300403 300543 300625 300824 300894 300911 301008 301135 301187 301252 301280 301332 301362 301578 600060 600261 600336 600619 600690 600839 600854 600983 601956 603075 603112 603215 603219 603303 603311 603355 603366 603486 603515 603519 603551 603578 603579 603657 603677 603726 603868 605336 605365 605555 688169 688609 688696 688793"
    "|食品饮料:000529 000568 000596 000639 000716 000729 000752 000799 000848 000858 000860 000869 000895 000929 000995 001215 001219 001318 002216 002304 002329 002330 002461 002495 002507 002515 002557 002568 002570 002582 002626 002646 002650 002661 002695 002702 002719 002726 002732 002820 002840 002847 002910 002946 002956 002991 003000 300106 300146 300783 300791 300892 300898 300908 300915 300973 300997 600059 600073 600084 600132 600186 600189 600197 600199 600238 600298 600300 600305 600381 600419 600429 600519 600530 600543 600559 600573 600597 600600 600616 600702 600779 600809 600872 600882 600887 601579 603027 603043 603057 603102 603156 603170 603198 603237 603262 603288 603317 603345 603369 603517 603536 603589 603696 603697 603711 603719 603755 603777 603779 603866 603886 603919 605089 605179 605300 605337 605338 605339 605388 605499 605567"
    "|纺织服饰:000017 000026 000726 000850 000955 001209 001234 001312 001390 002003 002029 002042 002083 002098 002144 002154 002193 002269 002293 002327 002345 002394 002397 002404 002486 002494 002563 002569 002574 002612 002634 002656 002674 002687 002721 002731 002762 002763 002780 002832 002867 002875 003016 003041 300005 300577 300591 300819 300840 300877 300901 300918 300945 300952 300979 301066 301088 301177 301276 600107 600137 600156 600177 600398 600400 600439 600448 600493 600527 600612 600630 600735 600916 600987 601339 601566 601599 601718 603001 603055 603116 603130 603196 603238 603307 603365 603511 603518 603557 603558 603587 603608 603665 603808 603839 603877 603889 603900 603908 603958 605003 605055 605138 605180 605189 605599"
    "|轻工制造:000488 000659 000812 000910 001211 001216 001221 001222 001238 001279 001300 001322 001323 001356 001368 001386 002012 002014 002067 002078 002084 002103 002117 002162 002191 002228 002235 002301 002303 002348 002374 002489 002521 002565 002571 002572 002599 002615 002631 002678 002701 002735 002752 002787 002790 002798 002799 002831 002836 002846 002853 002862 002899 002918 002969 003003 003011 003012 003018 300329 300501 300616 300640 300651 300703 300729 300749 300883 300993 301004 301011 301055 301061 301062 301101 301113 301193 301198 301223 301227 301287 301296 301335 301336 301355 301356 301376 301469 301501 301575 301588 301595 600076 600103 600210 600235 600308 600337 600356 600433 600567 600793 600963 600966 601515 601968 601996 603008 603021 603022 603058 603091 603165 603180 603195 603208 603216 603221 603226 603272 603284 603313 603326 603385 603389 603408 603429 603499 603600 603607 603610 603615 603661 603687 603709 603725 603733 603801 603816 603818 603833 603838 603848 603863 603898 603899 603992 605007 605080 605099 605155 605268 605299 605377 605500 688087"
    "|医药生物:000028 000078 000153 000403 000411 000423 000504 000513 000516 000518 000534 000538 000566 000590 000597 000623 000650 000661 000705 000710 000739 000756 000766 000788 000790 000813 000908 000915 000919 000931 000950 000952 000953 000963 000989 000999 001367 002004 002007 002019 002020 002022 002030 002038 002044 002082 002099 002102 002107 002172 002173 002198 002219 002223 002252 002262 002275 002287 002294 002317 002332 002349 002365 002370 002382 002390 002393 002399 002412 002422 002424 002432 002437 002462 002524 002550 002551 002566 002581 002589 002603 002622 002644 002653 002675 002693 002727 002728 002737 002742 002755 002773 002788 002793 002817 002821 002826 002864 002872 002873 002880 002900 002901 002907 002923 002932 002940 002950 003020 300003 300006 300009 300015 300016 300026 300030 300039 300049 300086 300110 300111 300122 300142 300143 300147 300149 300158 300171 300181 300194 300199 300204 300206 300233 300238 300239 300244 300246 300254 300255 300267 300289 300294 300298 300314 300318 300326 300341 300347 300357 300358 300363 300381 300396 300401 300404 300406 300430 300434 300436 300439 300452 300453 300463 300482 300485 300497 300519 300529 300534 300558 300562 300573 300583 300584 300595 300601 300633 300636 300639 300642 300653 300676 300677 300683 300685 300702 300705 300723 300725 300753 300759 300760 300765 300832 300841 300869 300878 300937 300942 300966 300981 301015 301017 301033 301047 301060 301065 301075 301080 301087 301089 301093 301096 301097 301103 301111 301122 301126 301130 301166 301188 301201 301207 301211 301230 301234 301235 301239 301246 301257 301258 301263 301267 301277 301281 301290 301293 301301 301331 301333 301363 301367 301370 301393 301408 301507 301509 301515 301520 301580 301584 301602 600055 600056 600062 600079 600080 600085 600129 600161 600196 600211 600216 600222 600252 600267 600272 600276 600285 600329 600332 600351 600380 600420 600422 600436 600479 600488 600511 600513 600518 600521 600529 600535 600538 600557 600566 600568 600572 600587 600594 600613 600624 600645 600664 600671 600713 600721 600739 600750 600763 600771 600774 600789 600812 600829 600833 600851 600867 600976 600993 600998 601089 601607 603014 603087 603108 603122 603127 603139 603168 603205 603207 603222 603229 603233 603259 603301 603309 603351 603367 603368 603387 603392 603439 603456 603520 603538 603567 603590 603658 603669 603676 603707 603716 603718 603811 603858 603880 603882 603883 603896 603939 603976 603987 603998 605116 605177 605199 605266 605369 605507 688013 688016 688026 688029 688046 688050 688062 688067 688068 688073 688075 688076 688085 688091 688105 688108 688114 688117 688131 688136 688137 688139 688151 688161 688163 688166 688176 688177 688180 688185 688189 688192 688193 688197 688198 688202 688212 688217 688221 688222 688235 688236 688238 688247 688253 688265 688266 688271 688273 688276 688277 688278 688289 688293 688298 688301 688302 688314 688315 688317 688319 688321 688331 688336 688338 688351 688356 688358 688366 688373 688382 688389 688393 688399 688410 688426 688428 688443 688468 688488 688505 688506 688513 688520 688553 688566 688575 688576 688578 688580 688581 688606 688607 688613 688617 688621 688626 688656 688658 688670 688677 688687 688690 688710 688712 688739 688755 688758 688759 688765 688767 688796 688799 688805 688806"
    "|公用事业:000027 000037 000155 000407 000507 000531 000537 000539 000543 000591 000593 000595 000600 000601 000631 000669 000690 000692 000722 000767 000791 000862 000875 000883 000899 000958 000966 000993 001210 001248 001258 001286 001289 001299 001331 001376 001896 002015 002039 002053 002218 002256 002259 002267 002310 002479 002480 002608 002616 002617 002700 002893 002911 003035 003816 300040 300125 300317 300332 300335 300483 600011 600021 600023 600025 600027 600032 600052 600098 600101 600116 600149 600157 600163 600167 600226 600236 600292 600310 600333 600396 600452 600475 600483 600505 600509 600575 600578 600617 600635 600642 600644 600674 600681 600719 600726 600744 600780 600795 600803 600821 600863 600868 600886 600900 600903 600905 600917 600930 600956 600969 600979 600982 600995 601016 601139 601619 601778 601908 601985 601991 603053 603071 603080 603105 603318 603393 603689 603693 603706 605011 605028 605090 605162 605169 605368 605580"
    "|交通运输:000088 000089 000099 000429 000520 000548 000557 000582 000626 000755 000828 000886 000900 000905 000906 001202 001205 001213 001220 001228 001317 001391 001872 001965 002010 002040 002120 002320 002352 002357 002468 002485 002492 002682 002769 002800 002889 002928 002930 300013 300240 300350 300873 600004 600009 600012 600017 600018 600020 600026 600029 600033 600035 600057 600106 600115 600119 600125 600153 600179 600180 600221 600233 600269 600279 600350 600368 600377 600428 600548 600561 600603 600611 600650 600704 600717 600751 600753 600755 600787 600794 600798 600834 600897 601000 601006 601008 601018 601021 601022 601083 601107 601111 601156 601188 601228 601298 601326 601333 601518 601598 601816 601866 601872 601880 601919 601975 603066 603069 603117 603128 603162 603167 603209 603329 603535 603565 603569 603648 603713 603813 603836 603871 603885 603967 605050"
    "|房地产:000002 000006 000011 000014 000029 000031 000036 000042 000056 000069 000090 000402 000514 000517 000560 000573 000608 000609 000620 000656 000668 000718 000736 000797 000838 000863 000897 000909 000926 000965 001914 001979 002016 002133 002146 002188 002208 002244 002285 002305 002314 002968 300917 600007 600048 600064 600082 600094 600136 600159 600162 600173 600208 600239 600246 600266 600322 600325 600340 600376 600383 600463 600503 600510 600515 600533 600604 600606 600622 600638 600639 600648 600649 600657 600658 600663 600665 600675 600683 600684 600692 600708 600716 600724 600736 600743 600748 600773 600791 600848 600895 601155 601512 601588 603506"
    "|商贸零售:000007 000058 000061 000151 000417 000419 000501 000564 000679 000701 000715 000759 000785 000829 000882 002024 002072 002091 002127 002177 002183 002187 002251 002277 002315 002344 002356 002416 002419 002561 002640 002697 002803 002818 003010 300022 300464 300518 300592 300622 300755 300792 300947 300972 301001 301078 301110 301381 301558 600058 600113 600128 600185 600278 600280 600287 600327 600365 600415 600539 600605 600628 600655 600693 600694 600697 600710 600712 600729 600738 600778 600785 600790 600814 600824 600827 600828 600838 600857 600858 600859 600865 600981 601010 601061 601086 601116 601366 601828 601888 601933 603101 603123 603214 603613 603682 603708 605136 605188"
    "|社会服务:000428 000430 000524 000526 000558 000610 000721 000796 000888 000978 002033 002057 002059 002093 002159 002186 002243 002306 002607 002627 002659 002707 002858 002967 003008 003032 300010 300012 300050 300144 300192 300215 300338 300359 300416 300492 300662 300688 300795 300797 300859 300860 300887 300938 300962 301073 301115 301169 301228 301289 301449 301508 301632 600054 600138 600158 600250 600258 600576 600593 600661 600662 600706 600730 600749 600754 600826 600861 600880 601007 603060 603099 603136 603183 603199 603373 603377 603402 605098 605108 688334 688757"
    "|综合:000025 000421 000523 000632 000652 000753 000833 002316 002420 002575 600051 600458 600620 600673 600682 600689 600770 600805 600807 600881"
    "|建筑材料:000012 000055 000401 000619 000672 000786 000789 000877 001212 002043 002066 002080 002088 002201 002205 002225 002233 002247 002271 002302 002333 002372 002392 002398 002457 002596 002613 002641 002652 002671 002694 002718 002785 002791 003037 300196 300198 300234 300374 300599 300715 300737 301010 301429 301526 600176 600293 600326 600425 600449 600585 600586 600668 600678 600801 600802 600819 600876 601112 601636 601992 603038 603256 603378 603601 603616 603737 603856 605006 605122 605318 688119"
    "|建筑装饰:000010 000032 000065 000498 000628 000779 000928 001396 002047 002051 002060 002061 002062 002081 002116 002135 002140 002163 002200 002307 002323 002375 002431 002469 002482 002541 002542 002586 002593 002620 002628 002663 002713 002717 002743 002761 002775 002789 002811 002822 002830 002856 002883 002941 002949 002963 002989 003001 003013 300197 300284 300355 300384 300500 300506 300517 300564 300621 300635 300649 300668 300675 300712 300732 300746 300778 300826 300844 300948 300949 300977 300982 300983 300986 300989 301024 301027 301038 301046 301058 301091 301098 301136 301167 301365 301390 301505 600039 600170 600234 600248 600284 600477 600491 600496 600502 600512 600629 600667 600720 600820 600846 600853 600939 600970 601068 601096 601117 601133 601186 601226 601390 601611 601618 601668 601669 601789 601800 601868 601886 603017 603018 603030 603098 603137 603153 603163 603176 603257 603357 603359 603458 603637 603815 603828 603843 603860 603909 603929 603955 603959 605167 605178 605287 605289 605303 605598"
    "|电力设备:000009 000049 000159 000400 000533 000682 000695 000720 000809 000821 000922 001208 001269 001283 001301 001382 001388 001393 002028 002052 002056 002058 002074 002079 002090 002112 002121 002125 002129 002150 002168 002169 002176 002196 002202 002227 002245 002249 002255 002270 002276 002300 002309 002335 002339 002340 002346 002350 002358 002364 002366 002441 002451 002452 002459 002471 002487 002498 002506 002518 002531 002533 002534 002546 002560 002576 002580 002606 002623 002630 002692 002706 002709 002733 002759 002801 002805 002812 002823 002850 002851 002857 002865 002879 002882 002892 002927 002951 002953 002980 003021 003022 300001 300014 300018 300035 300037 300051 300062 300068 300069 300073 300080 300093 300118 300129 300141 300153 300173 300185 300207 300222 300265 300274 300283 300286 300316 300340 300345 300360 300376 300390 300393 300407 300409 300410 300423 300427 300432 300438 300443 300444 300450 300457 300477 300490 300491 300510 300514 300530 300568 300569 300593 300617 300619 300626 300648 300660 300670 300690 300693 300713 300716 300724 300750 300751 300763 300769 300772 300776 300820 300827 300842 300850 300853 300870 300880 300882 300890 300904 300907 300919 300932 300933 300953 301002 301012 301023 301030 301040 301082 301120 301121 301150 301152 301155 301157 301163 301168 301179 301210 301217 301222 301226 301232 301238 301266 301278 301291 301292 301295 301325 301327 301349 301358 301361 301386 301388 301439 301456 301487 301502 301511 301513 301516 301557 301587 301590 301599 301609 301636 301658 301662 301668 301669 301696 301697 301699 600067 600072 600089 600110 600152 600192 600207 600212 600241 600268 600312 600379 600405 600406 600416 600438 600468 600478 600481 600482 600525 600537 600550 600577 600580 600590 600732 600847 600869 600875 600884 600885 600973 601012 601126 601179 601218 601222 601311 601567 601615 601616 601700 601727 601865 601877 603016 603026 603031 603032 603050 603063 603070 603092 603097 603185 603191 603212 603218 603248 603312 603320 603333 603344 603350 603370 603381 603396 603398 603489 603507 603530 603556 603577 603606 603618 603628 603659 603728 603778 603806 603819 603829 603861 603897 603906 603985 603988 605066 605117 605196 605222 605378 688005 688006 688032 688063 688116 688147 688148 688155 688184 688186 688191 688223 688226 688248 688275 688303 688330 688339 688345 688348 688349 688353 688388 688390 688392 688408 688411 688429 688450 688472 688499 688503 688516 688517 688551 688556 688560 688567 688573 688597 688599 688611 688616 688638 688660 688663 688676 688680 688681 688707 688717 688719 688726 688733 688772 688778 688779 688819"
    "|国防军工:000519 000547 000561 000576 000697 000733 000738 000768 001270 002023 002025 002111 002151 002179 002189 002214 002297 002338 002383 002389 002413 002414 002446 002465 002519 002625 002651 002829 002933 002935 002977 002985 003009 300008 300034 300045 300065 300101 300123 300177 300252 300395 300397 300424 300447 300474 300527 300581 300589 300600 300629 300696 300699 300719 300722 300726 300762 300775 300777 300810 300875 300900 300922 300965 301050 301213 301302 301306 301357 301517 301571 301586 301592 301682 301707 302132 600038 600118 600150 600184 600316 600372 600391 600435 600562 600685 600760 600764 600765 600855 600862 600879 600893 600967 600990 601606 601698 601890 603261 603267 603268 603678 603712 605123 688011 688053 688070 688081 688084 688122 688132 688175 688237 688239 688272 688281 688282 688297 688311 688375 688439 688510 688511 688522 688523 688543 688552 688563 688586 688629 688636 688682 688685 688708 688776 688788 688818"
    "|计算机:000034 000066 000158 000409 000503 000555 000815 000938 000948 000977 000997 001229 001339 002063 002065 002152 002153 002178 002180 002195 002197 002212 002229 002230 002232 002236 002253 002261 002268 002279 002298 002322 002331 002362 002368 002373 002376 002380 002401 002405 002410 002415 002421 002439 002474 002512 002528 002577 002609 002642 002649 002657 002771 002777 002835 002869 002908 002912 002920 002970 002987 002990 003004 003005 003007 003029 300017 300020 300033 300036 300042 300044 300047 300074 300075 300078 300079 300085 300096 300130 300150 300155 300166 300167 300168 300170 300188 300209 300212 300226 300229 300231 300235 300245 300248 300249 300250 300253 300264 300270 300271 300275 300277 300287 300288 300290 300300 300302 300311 300324 300333 300339 300348 300352 300365 300366 300368 300369 300377 300378 300380 300386 300399 300419 300440 300448 300449 300451 300454 300455 300462 300465 300468 300469 300479 300496 300508 300520 300523 300525 300531 300532 300536 300541 300542 300546 300550 300551 300552 300556 300559 300561 300579 300588 300598 300605 300608 300609 300624 300634 300645 300659 300663 300674 300678 300682 300687 300691 300730 300743 300766 300768 300771 300789 300790 300803 300807 300830 300845 300846 300847 300872 300884 300895 300925 300935 300941 300996 301042 301085 301095 301117 301153 301159 301162 301172 301178 301182 301185 301195 301208 301218 301236 301248 301269 301270 301315 301316 301318 301330 301337 301339 301378 301391 301396 301503 301556 301589 301600 301608 301638 600100 600131 600271 600288 600410 600446 600455 600476 600536 600570 600571 600588 600589 600602 600654 600666 600718 600728 600756 600797 600845 600850 601360 601519 603019 603039 603106 603138 603171 603189 603232 603383 603496 603508 603516 603528 603636 603660 603859 603869 603887 603918 603927 603990 605118 605398 688004 688023 688030 688031 688038 688039 688051 688058 688060 688066 688078 688083 688088 688095 688109 688111 688118 688152 688158 688168 688171 688188 688201 688206 688207 688208 688225 688227 688228 688229 688232 688244 688246 688251 688258 688288 688291 688292 688296 688316 688318 688326 688327 688343 688365 688369 688435 688475 688479 688489 688500 688507 688509 688561 688562 688568 688579 688588 688590 688615 688619 688631 688651 688657 688692 688695 688787"
    "|传媒:000156 000607 000665 000676 000681 000719 000793 000802 000892 000917 001330 002027 002095 002174 002181 002238 002264 002291 002292 002343 002354 002400 002425 002517 002555 002558 002602 002605 002624 002712 002739 002878 002905 002919 002995 300002 300027 300031 300043 300052 300058 300061 300063 300071 300113 300133 300148 300182 300242 300251 300291 300295 300299 300315 300364 300413 300418 300426 300459 300467 300494 300528 300533 300612 300654 300770 300781 300785 300788 300805 300987 301025 301052 301102 301171 301231 301262 301299 301313 301551 600037 600088 600228 600229 600358 600373 600551 600556 600633 600637 600640 600715 600757 600825 600831 600892 600936 600959 600977 600986 600996 601019 601098 601595 601801 601811 601858 601900 601921 601928 601929 601949 601999 603000 603096 603103 603230 603258 603444 603466 603533 603598 603602 603721 603729 603825 603888 603999 605168 605577"
    "|通信:000063 000070 000586 000839 000889 001267 002017 002104 002115 002123 002148 002194 002281 002296 002313 002396 002467 002491 002544 002583 002792 002796 002881 002897 002902 002929 003031 003040 300025 300081 300098 300134 300183 300205 300211 300213 300292 300308 300310 300353 300383 300394 300414 300442 300502 300504 300513 300548 300555 300560 300563 300565 300570 300571 300578 300590 300597 300603 300615 300620 300627 300628 300638 300689 300698 300710 300711 300738 300913 300921 300959 301139 301165 301191 301202 301205 301314 301380 301382 301419 301428 301577 600050 600105 600198 600289 600345 600487 600498 600522 600734 600775 600776 600941 601728 601869 603042 603083 603118 603206 603220 603236 603322 603421 603559 603803 603881 688027 688080 688100 688143 688159 688182 688205 688283 688313 688387 688418 688592 688618 688635 688668"
    "|银行:000001 001227 002142 002807 002839 002936 002948 002958 002966 600000 600015 600016 600036 600908 600919 600926 600928 601009 601077 601128 601166 601169 601187 601229 601288 601328 601398 601528 601577 601658 601665 601818 601825 601838 601860 601916 601939 601963 601988 601997 601998 603323"
    "|非银金融:000166 000415 000532 000563 000567 000617 000686 000712 000728 000750 000776 000783 000935 000987 001236 002423 002500 002647 002670 002673 002736 002797 002926 002939 002945 002961 300059 300773 600030 600053 600061 600095 600109 600120 600155 600318 600369 600390 600517 600621 600643 600816 600830 600864 600901 600906 600909 600918 600927 600958 600999 601059 601066 601099 601108 601136 601162 601198 601211 601236 601318 601319 601336 601375 601377 601456 601555 601601 601628 601688 601696 601788 601878 601881 601901 601990 601995 603093 603300"
    "|汽车:000030 000338 000550 000559 000570 000572 000581 000589 000599 000625 000678 000700 000757 000800 000816 000868 000887 000901 000903 000913 000951 000957 000980 000981 001233 001260 001278 001282 001285 001311 001319 001365 001380 002048 002085 002101 002105 002126 002190 002239 002265 002283 002284 002328 002355 002363 002406 002434 002448 002454 002472 002488 002510 002516 002536 002537 002547 002553 002590 002592 002594 002662 002664 002703 002708 002715 002725 002765 002766 002806 002813 002863 002870 002906 002921 002937 002965 002976 002984 002997 003033 300100 300176 300237 300258 300304 300375 300428 300446 300473 300507 300547 300580 300585 300611 300643 300652 300680 300681 300694 300695 300707 300733 300745 300816 300825 300863 300893 300912 300926 300928 300969 300978 300994 300998 301000 301005 301007 301020 301022 301039 301072 301119 301133 301170 301173 301181 301186 301192 301215 301221 301225 301229 301233 301298 301310 301322 301345 301397 301398 301488 301499 301529 301535 301539 301550 301552 301560 301581 301607 301613 301631 301655 301656 301667 301680 600006 600066 600081 600099 600104 600148 600151 600166 600178 600182 600218 600303 600335 600375 600386 600418 600469 600480 600523 600609 600626 600651 600653 600660 600676 600679 600686 600698 600699 600733 600741 600742 600818 600822 600841 600933 600960 601058 601127 601163 601238 601279 601500 601633 601689 601777 601799 601965 601966 603006 603007 603009 603013 603023 603035 603037 603040 603048 603049 603085 603089 603107 603109 603119 603121 603129 603158 603161 603166 603178 603179 603190 603197 603201 603202 603210 603211 603239 603266 603286 603293 603305 603306 603319 603335 603348 603352 603358 603376 603390 603409 603418 603448 603468 603529 603586 603596 603655 603701 603730 603758 603766 603767 603768 603776 603786 603787 603788 603809 603917 603922 603926 603949 603950 603960 603982 603997 605005 605018 605068 605088 605128 605133 605151 605228 605255 605319 605333 688021 688280 688533 688612 688667 688737"
    "|机械设备:000008 000039 000157 000410 000425 000528 000530 000680 000777 000811 000837 000852 000856 000880 000925 000927 000988 001223 001225 001226 001237 001239 001256 001266 001277 001288 001306 001324 001332 001360 001379 001395 001400 001696 002006 002008 002009 002021 002026 002031 002046 002073 002097 002122 002131 002132 002158 002164 002175 002184 002204 002209 002248 002272 002278 002282 002334 002337 002342 002347 002353 002367 002438 002444 002445 002483 002490 002514 002520 002523 002526 002527 002529 002530 002535 002559 002564 002595 002598 002611 002633 002639 002667 002685 002686 002689 002690 002691 002698 002722 002730 002747 002757 002760 002767 002774 002779 002786 002795 002816 002819 002833 002837 002843 002849 002871 002877 002884 002890 002896 002903 002931 002943 002957 002960 002972 002975 002979 003025 003036 300004 300007 300011 300024 300048 300066 300083 300084 300091 300092 300095 300097 300099 300103 300112 300124 300126 300145 300151 300154 300157 300159 300161 300165 300179 300193 300195 300201 300220 300227 300228 300257 300259 300260 300263 300276 300278 300281 300293 300306 300307 300349 300351 300354 300370 300371 300382 300400 300402 300411 300412 300415 300417 300420 300421 300435 300441 300445 300461 300466 300470 300471 300472 300480 300484 300486 300488 300499 300503 300509 300512 300515 300521 300540 300549 300553 300554 300557 300567 300572 300594 300606 300607 300667 300669 300700 300718 300720 300747 300756 300757 300780 300802 300809 300813 300817 300818 300823 300828 300833 300836 300837 300838 300851 300861 300862 300865 300879 300885 300897 300902 300906 300923 300931 300943 300946 300950 300960 300971 300984 300985 300988 300990 300992 301006 301013 301016 301018 301021 301028 301029 301032 301043 301048 301053 301056 301063 301070 301079 301083 301105 301107 301112 301125 301128 301129 301137 301138 301151 301158 301160 301161 301197 301199 301200 301255 301260 301261 301268 301272 301273 301279 301303 301307 301309 301311 301312 301317 301320 301338 301353 301360 301368 301377 301392 301399 301413 301418 301446 301448 301468 301510 301512 301525 301528 301533 301548 301559 301568 301596 301598 301601 301603 301616 301633 301677 301689 600031 600114 600169 600172 600202 600215 600232 600243 600262 600302 600320 600343 600397 600444 600495 600499 600501 600520 600528 600545 600558 600560 600579 600582 600592 600761 600783 600815 600835 600843 600860 600889 600894 600980 600984 600992 601002 601038 601100 601106 601177 601369 601399 601608 601717 601766 601798 601882 603011 603012 603015 603025 603028 603029 603036 603076 603081 603082 603088 603090 603095 603100 603111 603131 603135 603159 603169 603173 603187 603194 603203 603269 603270 603273 603275 603277 603278 603279 603280 603283 603289 603298 603308 603321 603325 603331 603337 603338 603339 603356 603391 603400 603416 603488 603500 603583 603611 603617 603638 603656 603662 603666 603667 603680 603698 603699 603700 603757 603789 603800 603855 603895 603901 603912 603915 603956 603966 603969 605001 605056 605060 605100 605186 605259 605286 605288 605298 605305 605389 688001 688003 688009 688015 688017 688022 688025 688028 688033 688056 688059 688071 688090 688092 688097 688112 688113 688115 688121 688125 688128 688160 688162 688165 688170 688187 688211 688215 688218 688255 688257 688285 688290 688305 688306 688308 688310 688312 688320 688328 688329 688333 688337 688355 688360 688367 688377 688379 688383 688395 688400 688420 688425 688433 688448 688455 688459 688485 688518 688528 688529 688531 688557 688558 688559 688569 688570 688577 688583 688596 688600 688610 688622 688623 688627 688628 688630 688633 688646 688648 688665 688686 688697 688698 688700 688768 688777 688789 688808 688813 688816 688826 688828 688835 688836"
    "|煤炭:000552 000571 000723 000937 000983 002128 600121 600123 600188 600348 600395 600403 600408 600508 600546 600725 600740 600758 600792 600925 600971 600985 600997 601001 601011 601015 601088 601101 601225 601666 601699 601898 601918"
    "|石油石化:000059 000096 000301 000554 000637 000698 000703 000819 000968 000985 001316 002207 002221 002377 002408 002476 002493 002554 002629 002828 002986 300055 300135 300164 300191 300839 600028 600256 600339 600346 600506 600583 600688 600759 600777 600800 600871 600938 600968 601233 601808 601857 603223 603353 603619 603727 603798"
    "|环保:000035 000068 000544 000546 000551 000598 000605 000685 000711 000803 000820 000826 000885 000890 000967 001230 001336 002034 002210 002266 002573 002645 002658 002672 002778 002887 002973 003027 003039 300056 300070 300105 300137 300140 300152 300172 300187 300190 300203 300210 300266 300334 300385 300388 300422 300425 300437 300614 300631 300664 300692 300774 300779 300786 300800 300815 300854 300864 300867 300899 300929 300958 300961 301049 301068 301081 301109 301127 301148 301175 301203 301259 301265 301288 301305 301372 301500 301519 600008 600133 600168 600187 600217 600283 600323 600388 600461 600526 600769 600796 600817 600874 601033 601158 601199 601200 601330 601368 601827 603126 603177 603200 603282 603291 603315 603324 603568 603588 603686 603759 603797 603817 603903 605069 688057 688069 688096 688101 688178 688309 688335 688370 688376 688466 688480 688501 688565 688671 688679 688701"
    "|美容护理:000615 001206 001328 002094 002511 003006 300132 300658 300740 300849 300856 300888 300896 300955 300957 301009 301108 301371 600223 600249 600315 603059 603193 603605 603630 603983 605009 688363"
)
# === EMB_IND_END ===
# === EMB_CON_BEGIN ===
# 概念层（当前策略未使用，留空以省体积）
# 需要时: python gen_embedded_map.py --kind concept
EMB_CONCEPT_RAW = (
    ""
)
# === EMB_CON_END ===
MINUTE_BARS_TODAY = 61              # 10:30 时当日 1 分钟线根数（09:30~10:30）；改扫描时点须同步改
ASSUME_ENTRY_PREMIUM = 0.010        # 当日价取不到时的保守假设加成（001 口径：昨收×1.010）
DIAG_ONCE        = True             # 首个交易日输出一次 [诊断]，之后不再刷屏
MIN_AMOUNT       = 4e8              # 成交额下限（3亿~5亿折中）
MIN_VOL_RATIO    = 1.5
REQUIRE_ABOVE_MA20 = True
REQUIRE_ABOVE_MA60 = True
MA_SHAPE_MIN     = 2                # 均线多头形态阈值（lhb888）
INTRADAY_PULLBACK_MAX = 0.04        # 日内回撤上限（lhb888）
ALLOW_LIMIT_UP_BUY  = False
REQUIRE_NO_ST    = True
DETAIL_POOL      = 60               # 细评池上限
MAX_CANDIDATES   = 12

# --- 买入/仓位 ---
POSITION_VALUE_RATIO = 0.17
STRENGTH_EXP     = 1.6
MAX_SINGLE_RATIO = 0.25
MAX_POSITIONS    = 6
MAX_NEW_PER_DAY  = 2                # 放宽001的1笔，避免踏空；仍控频
COOL_DOWN_DAYS   = 2                # 比001的3日更短
CASH_BUFFER      = 0.10
BUY_FLOOR_PCT    = 0.015           # 走弱不接刀（001双护栏）
BUY_PREMIUM_PCT  = 0.03            # 涨过头不追（001双护栏）
BUDGET_RECYCLE   = True
RECYCLE_ROUNDS   = 3
REINVEST_ON_SELL = False            # 砍仓额度留次日（1030）

# --- 止损 ---
ATR_N            = 14
ATR_STOP_MULT    = 1.8            # 调优：略降倍数贴近真实波动，减少窄幅震荡被止损
ATR_STOP_MIN_PCT = 0.06            # 下限6%（v1.4=5%，略放宽避免微幅下挫即砍）
ATR_STOP_MAX_PCT = 0.11            # 上限11%（放宽001的6%减震）
STOP_TIME_FLOOR  = "09:45"          # 硬止损最早时点（避集合竞价噪声）
STOP_MODE        = "atr"           # 亦可 "fixed"
FIXED_STOP_PCT   = 0.05

# --- 止盈 ---
PARTIAL_TAKE_PCT = 0.10            # +10%减半（1030）
PARTIAL_TAKE_RATIO = 0.5
BREAKEVEN_GUARD  = 0.03            # 浮盈>=3%锁保本（lhb888）
TRAIL_ARM_PCT    = 0.05            # 移动止盈武装（001）
TRAIL_PCT        = 0.15            # 峰值回撤15%清仓（001）
TP_FULL_PCT      = 0.30            # +30%全清（lhb888）

# --- 破均线 ---
RETREAT_MA_FAST  = 10              # 破10日线（浮亏才砍）
RETREAT_MA_HARD  = 20              # 破20日线（硬清）

# --- 时间/赢家 ---
MAX_HOLD_DAYS    = 5               # 亏损持仓>=5日退出
WINNER_EXEMPT_PCT   = 0.12         # 浮盈>=15%进入赢家粘性（001 V4.10）
WINNER_GIVEBACK_PCT= 0.10          # 自峰值回撤>=8pp清仓（百分点尺，更稳）
WINNER_MAX_HOLD  = 30             # 赢家最长持有天数

# --- 大市择时/避险 ---
REGIME_BROAD     = "000300.SS"     # 沪深300（宽基，暂停新建用）
REGIME_SMALL     = "000852.SS"     # 中证1000（与持仓风格匹配，halt用）
REGIME_MA        = 20
REGIME_MA_FAST   = 5
REGIME_LAYERED   = True      # v1.6：True=分层(双弱才HALT/单弱HALF)；False=回退旧口径(任一破MA20即HALT/PAUSE)
MARKET_ZT_FLOOR  = 25              # 池内涨停<25 -> 情绪冰点HALF（1030）
SENTIMENT_HALF_FACTOR = 0.5        # HALF模式预算系数

# --- 换手熔断 ---
MAX_ANNUAL_TURNS = 25
TURNOVER_WINDOW_DAYS = 60
MAX_BUYS_PER_MONTH = 20

ALLOW_BOARDS = ("SS", "SZ")
BROAD_TAG_BLACKLIST = ()           # 可扩展宽口径标签


# ----------------------------- 工具函数 -------------------------------------
def _suffix(code):
    if "." in code:
        return code
    if code.startswith("6") or code.startswith("9"):
        return code + ".SS"
    return code + ".SZ"


def _pure(code):
    return code.split(".")[0]


def _board(code):
    c = _pure(code)
    if c.startswith("8") or c.startswith("4"):
        return "BJ"
    if c.startswith("68"):
        return "KCB"
    if c.startswith("30"):
        return "CYB"
    return "MB"


def _is_kcb(code):
    return _board(code) == "KCB"


def _limit_pct(code):
    b = _board(code)
    if b == "KCB":
        return 0.20
    if b == "BJ":
        return 0.30
    return 0.10


def _gain_range(code):
    # v1.8 对齐 lhb888 v3.5 的 GAIN_RANGE（收窄上限 = 降追高）：
    #   主板 3%~4%；科创/创业板(20cm) 6%~8%。
    # v1.7 及以前是 主板 3.5%~6% / 创科 6%~9%，买得比 lhb888 更晚更高。
    # 实测 32 笔卖出里 27 笔为亏损退出，其中硬止损 16 笔均值 -7.58%（占毛亏 95%）；
    #   单笔胜率 15.6%、盈亏比 0.36、单笔期望 -310 元/笔 —— 追高被埋的典型特征。
    b = _board(code)
    if b == "KCB":
        return (0.06, 0.08)
    if b == "CYB":
        return (0.06, 0.08)
    return (0.03, 0.04)


def _flatten(vals):
    """把 get_history 的 .values 展平成一维浮点序列。

    多码+单字段时 PTrade 常返回 [[v],[v],...]（每行一列），直接 sum() 会
    触发 'int + list' 异常。这里统一兼容 标量 / 单元素行 / 多列行。
    """
    out = []
    if vals is None:
        return out
    for v in vals:
        try:
            if isinstance(v, (list, tuple)):
                out.append(float(v[0]))
            else:
                out.append(float(v))
        except Exception:
            continue
    return out


def _ma(vals, n):
    if vals is None or len(vals) < n:
        return None
    seq = _flatten(vals[-n:])
    if len(seq) < n:
        return None
    return sum(seq) / float(n)


def _mean(xs):
    if not xs:
        return 0.0
    return sum(xs) / float(len(xs))


def _median(xs):
    if not xs:
        return 0.0
    s = sorted(xs)
    m = len(s)
    if m % 2 == 1:
        return s[m // 2]
    return (s[m // 2 - 1] + s[m // 2]) / 2.0


def _has(name):
    try:
        return name in globals() or callable(globals().get(name))
    except Exception:
        return False


def _warn(msg):
    try:
        log.warning(msg)
    except Exception:
        pass


def _warn_once(key, msg):
    g = _G()
    slot = g.setdefault("noise_slot", {})
    today = g.get("today", "")
    d = slot.setdefault(key, {})
    if d.get("day") == today:
        return
    d["day"] = today
    _warn(msg)


def _days_between(a, b):
    # a,b: 'YYYY-MM-DD'，返回日历日差（近似交易日用）
    try:
        ya, ma, da = [int(x) for x in a.split("-")]
        yb, mb, db = [int(x) for x in b.split("-")]
        import datetime
        da_dt = datetime.date(ya, ma, da)
        db_dt = datetime.date(yb, mb, db)
        return abs((db_dt - da_dt).days)
    except Exception:
        return 999


# ----------------------------- 平台接口封装 --------------------------------
def _G():
    return globals().get("g", {})


def _hist(code, count, fields):
    """单只批量取数：get_history(count, freq, field, [code]) -> 该只的 DataFrame

    注意：PTrade 的 get_history 在 securities 传入【列表】时恒返回 dict
    （key=标的）。本函数统一把单只的 DataFrame 抽取出来返回，避免上层
    _hist_closes / _stop_pct 误把整个 dict 当 DataFrame 用（raw.values 会
    变成 dict_values 导致取序列失败 -> ATR自适应止损与破均线出场双双失效）。
    同时兼容平台对单只也直接返回 DataFrame 的情形。
    """
    try:
        if not _has("get_history"):
            return None
        fields = _norm_fields(fields)
        if not fields:
            return None
        raw = _hist_call(count, "1d", fields, [_suffix(code)])
        if raw is None:
            return None
        if isinstance(raw, dict):
            if _suffix(code) in raw:
                return raw[_suffix(code)]
            if _pure(code) in raw:
                return raw[_pure(code)]
            vals = list(raw.values())
            return vals[0] if vals else None
        return raw
    except Exception as e:
        _warn("[hist] %s 取数失败: %s" % (code, e))
        return None


def _account(context):
    # PTrade 无 get_account；读 context.portfolio
    try:
        pf = context.portfolio
        return float(pf.portfolio_value), float(pf.cash)
    except Exception as e:
        _warn("[acct] %s" % e)
        return 0.0, 0.0


def _get_positions(context):
    """兼容 dict[str:Position] 与 list[dict] 两种形态（防001/lhb888同源缺陷）"""
    try:
        poss = context.portfolio.positions
    except Exception:
        try:
            poss = get_positions()
        except Exception:
            return []
    if isinstance(poss, dict):
        out = []
        for k, v in poss.items():
            out.append(_pos_to_dict(v, k))
        return out
    if isinstance(poss, list):
        out = []
        for p in poss:
            out.append(_pos_to_dict(p, ""))
        return out
    return []


def _pos_to_dict(p, code_hint):
    d = {}
    if isinstance(p, dict):
        d = dict(p)
        if not code_hint and "stock_code" in d:
            code_hint = d["stock_code"]
        if not code_hint and "sid" in d:
            code_hint = d["sid"]
    else:
        for nm in ("sid", "stock_code", "code"):
            if hasattr(p, nm):
                code_hint = getattr(p, nm)
                break
        for nm in ("amount", "current_amount", "enable_amount", "cost_basis",
                  "last_sale_price", "last_price", "cost_price", "avg_price"):
            if hasattr(p, nm):
                d[nm] = getattr(p, nm)
    d["_code"] = _suffix(code_hint) if code_hint else ""
    return d


def _pos_field(p, names):
    for nm in names:
        if nm in p and p[nm] is not None:
            try:
                return float(p[nm])
            except Exception:
                return 0.0
    return 0.0


def _pick(obj, names):
    """从对象或 dict 中取第一个存在字段并转 float；取不到返回 None。"""
    if obj is None:
        return None
    for k in names:
        v = None
        try:
            if isinstance(obj, dict):
                v = obj.get(k)
            else:
                v = getattr(obj, k, None)
        except Exception:
            v = None
        if v is not None:
            try:
                return float(v)
            except Exception:
                continue
    return None


# ============ 当日价读取（v1.4 根因修复：数值优先，绝不只认字段名）============
def _num(v):
    """转正 float；非数或 <=0 一律 None。"""
    try:
        f = float(v)
    except Exception:
        return None
    return f if f > 0 else None


def _bar_price(obj, prev_close=None):
    """从一个 Bar 对象取「当日价」——按候选顺序挑【不等于昨收】的第一个值。

    【为什么数值优先、且要过滤昨收】2026-09-22 实测（1030 日志第 15 行原文）：
        样本 000001.SZ 昨收=11.13 data价=11.34 price字段=11.13 分钟close=11.13
    即：同一只票，data 对象【数值本身】= 当日价 11.34，而按 `price` 字段名读到的是
    昨收 11.13。v1.3 的 _cur_bar 只按字段名读（price/last_px/close）→ 当日价恒=昨收
    → pct 恒 0 → 涨幅区间 [3.5%,6%] 全灭 → 粗筛恒空 → 全天 0 成交。
    这里不仅要求"先试数值"，还要逐候选与昨收比对：只有明显不等于昨收的值才算
    "当日价"，否则继续往下找（找不到就交给上层走假设定价）。
    """
    if obj is None:
        return None
    eps = 1e-9
    cands = []
    v = _num(obj)                      # ① Bar 可转 float → 数值就是当日价（1030 实证）
    if v:
        cands.append(v)
    for f in ("close", "price", "last_price", "lastPrice", "last_px", "new_price"):
        try:
            v = _num(obj.get(f)) if isinstance(obj, dict) else _num(getattr(obj, f, None))
        except Exception:
            v = None
        if v:
            cands.append(v)
    fn = getattr(obj, "values", None)
    if callable(fn):
        try:
            for x in list(fn()):
                v = _num(x)
                if v:
                    cands.append(v)
        except Exception:
            pass
    for v in cands:
        if prev_close and prev_close > 0:
            if abs(v - prev_close) > eps:
                return v
        else:
            return v
    return None


def _data_price(data, code, prev_close=None):
    """handle_data 的 data 对象 -> 当日价；取不到返回 None。

    入口组合照抄 1030（同平台、同回测配置，实测 5098/5201 只拿得到当日价）：
      数值(data.get(code)) -> 数值(data[code]) -> data.current(code, field)
      -> data.get_price(...)
    每一档都用 prev_close 过滤掉"其实是昨收"的值 —— 因为本平台【按字段名】取到的
    很可能是昨收，而数值才是当日价。
    """
    if data is None:
        return None
    keys = (_suffix(code), _pure(code))
    # ① 数值优先：data.get(code) / data[code]
    for k in keys:
        obj = None
        try:
            if hasattr(data, "get"):
                obj = data.get(k)
        except Exception:
            obj = None
        if obj is None:
            try:
                obj = data[k]
            except Exception:
                obj = None
        v = _bar_price(obj, prev_close)
        if v:
            return v
    # ② 方法式入口
    for m in ("current", "get_price"):
        fn = getattr(data, m, None)
        if not callable(fn):
            continue
        for k in keys:
            for f in ("price", "close", "last_price", "lastPrice"):
                try:
                    v = _num(fn(k, f))
                except Exception:
                    v = None
                if v and (not prev_close or abs(v - prev_close) > 1e-9):
                    return v
    return None


def _today_price(data, code, prev_close, daily_price, minute_price):
    """当日价四层兜底 -> (价格, 来源标签)。

    【v1.4 铁律】每一层都要做「与昨收相等就作废」的 guard（1030 源码 1186/1188 行
    的已验证写法）。没有这道闸，本平台会把「昨收」当「当日价」交给你，而 pct 恒 0
    不会报任何错 —— v1.3 全天 0 成交就是栽在这里。
    四层：data 对象 -> 日线 price 字段 -> 分钟线 -> 保守假设定价。
    最后一层【不弃票】（001 口径），保证回测不至全池为空。
    """
    eps = 1e-9
    if prev_close and prev_close > 0:
        for v, tag in ((_data_price(data, code, prev_close), "data"),
                       (_num(daily_price), "price字段"),
                       (_num(minute_price), "分钟线")):
            if v and abs(v - prev_close) > eps:
                return v, tag
        return prev_close * (1.0 + ASSUME_ENTRY_PREMIUM), "假设定价"
    return None, "无昨收"


def _cur_bar(data, code, snap=None):
    """取【当日】快照：价/开/高/低/量。

    口径说明（v1.3 已改为运行时判定，不再假设）：本平台日线【是否含当日 bar】
    与策略时点相关 —— 001 日志实测含当日（故其有 _strip_today），1030 源码注释
    写不含当日。两者矛盾，所以 scan_market 改为读 date 列自行判定：
      · 含当日 → 当日价直接用末根，根本不会走到这个函数；
      · 不含当日 → 末根=昨收，当日价/开/高/量必须另找来源，优先级：
        handle_data 的 data 对象 -> 批量 get_snapshot -> 调用方再兜分钟线。
    取不到时由调用方走「保守假设定价」，绝不用昨收冒充当日价（否则涨幅恒为 0，
    静默选出全错的一批票）。
    """
    code_s = _suffix(code)
    out = {"price": None, "open": None, "high": None, "low": None, "volume": None}
    obj = None
    if data is not None:
        try:
            obj = data.get(code_s) if hasattr(data, "get") else None
            if obj is None and hasattr(data, "__getitem__"):
                obj = data[code_s]
        except Exception:
            obj = None
    for src in (obj, snap):
        if src is None:
            continue
        if out["price"] is None:
            out["price"] = _pick(src, ("price", "last_px", "last_price", "close"))
        if out["open"] is None:
            out["open"] = _pick(src, ("open", "open_price", "today_open"))
        if out["high"] is None:
            out["high"] = _pick(src, ("high", "high_price", "today_high"))
        if out["low"] is None:
            out["low"] = _pick(src, ("low", "low_price", "today_low"))
        if out["volume"] is None:
            out["volume"] = _pick(src, ("volume", "vol", "business_amount"))
    return out


def _snap_map(pool):
    """批量实时快照 -> {code: {..}}，失败或不可用返回 {}。

    注意：PTrade **回测**环境不支持 get_snapshot（平台会直接打
    「回测不支持get_snapshot函数」），所以回测里本函数恒为空，
    当日价只能靠 data 快照或批量分钟线 —— 别把快照当唯一来源。
    """
    out = {}
    if not _has("get_snapshot"):
        return out
    try:
        snap = get_snapshot(list(pool))
    except Exception:
        return out
    if not snap:
        return out
    try:
        for code, v in snap.items():
            out[str(code)] = v
            out[_pure(code)] = v
    except Exception:
        pass
    return out


def _split_frame(part, chunk):
    """MultiIndex 列 DataFrame -> {6位code: 该只的子DataFrame}；不适用返回 {}。

    本平台传 is_dict=True 时返回 dict，走不到这里；但若某版本忽略 is_dict 而退回
    多证券 DataFrame，这一步是唯一能救回来的地方——否则整批被当作"无法识别"作废。
    """
    cols = getattr(part, "columns", None)
    if cols is None:
        return {}
    out = {}
    try:
        cl = list(cols)
        if getattr(cols, "nlevels", 1) < 2 or not cl:
            return {}
        codeset = set(_pure(c) for c in chunk)
        hit = {}
        for lev in (0, 1):
            s = set()
            for c in cl:
                try:
                    s.add(_pure(c[lev]))
                except Exception:
                    pass
            hit[lev] = len(s & codeset)
        lv = 0 if hit[0] >= hit[1] else 1
        for c in cl:
            key = _pure(c[lv])
            if not key:
                continue
            out.setdefault(key, []).append(c)
        res = {}
        for key, clist in out.items():
            try:
                res[key] = part[clist]
            except Exception:
                pass
        return res
    except Exception:
        return {}


def _fetch_panel(fields, codes, count=None, diag=False):
    """分批 get_history（'1d'）并合并 -> (dict{code: sub}, 批次数)。

    v1.3 两条硬纪律：
      ① 走 _hist_call（带形态探测 + is_dict=True + fq 降级），不再裸调 get_history；
      ② 无论返回什么形态，【要么被识别、要么显式报错】—— 旧版把"形态不匹配"
         静默当成"没有数据"，把排查方向带偏了两整轮，这是不可再犯的错。
    分批保留（FETCH_BATCH=200）：单批过大平台会静默截断，且单批失败不波及其它批。
    """
    out = {}
    nbatch = 0
    total = len(codes)
    n = MAX_HIST_BARS + 5 if count is None else count
    i = 0
    while i < total:
        chunk = list(codes[i:i + FETCH_BATCH])
        i += FETCH_BATCH
        part = _hist_call(n, "1d", fields, chunk)
        nbatch += 1
        if part is None:
            if nbatch <= 2:
                log.error("[取数] 第 %d 批（%d 只）返回 None（形态=%s）"
                          % (nbatch, len(chunk), _DS.get("style_name")))
            continue
        # 形态 1：{code: sub}（本平台 is_dict=True 的正常形态，001 已实测）
        if isinstance(part, dict):
            for k in list(part.keys()):
                out[k] = part[k]
                kk = _pure(k)
                if kk:
                    out[kk] = part[k]
            continue
        # 形态 2：MultiIndex 列 DataFrame -> 按代码拆
        sp = _split_frame(part, chunk)
        if sp:
            for k, v in sp.items():
                out[k] = v
            continue
        # 形态 3：按请求顺序返回的序列
        if isinstance(part, (list, tuple)):
            for j, v in enumerate(part):
                if j < len(chunk):
                    out[chunk[j]] = v
            continue
        # 形态 4：认不出来 —— 必须吼出来（旧版就是在这里静默丢光的）
        if nbatch <= 2:
            log.error("[取数] 第 %d 批返回了无法识别的形态 type=%s repr=%s → 本批作废"
                      % (nbatch, type(part).__name__, repr(part)[:160]))
            log.error("[取数] 把这行发我，需要为这个形态补一个适配分支（别改批大小，没用）")
    if diag and total and not _DS["diag"]:
        _DS["diag"] = True
        _diag_panel(out, codes, fields, nbatch)
    return out, nbatch


def _minute_map(fields, codes, bars=MINUTE_BARS_TODAY):
    """批量分钟线 -> {code: {price/open/high/low/volume}}，聚合成「当日至今」一根 bar。

    回测不支持 get_snapshot，但**分钟线可得**（001 源码注释：日线不含当日 bar，
    分钟线可得）。因此这是回测里唯一能拿到「当日价」的批量通道。逐只调用太慢
    （5000+ 次），必须分批。

    聚合口径（重要）：
      price  = 最后一根分钟 close（当日最新价）
      open   = 第一根分钟 open（当日开盘）
      high/low = 窗口内 max/min
      volume = 窗口内 sum ⇒ 即「当日累计成交量」，可直接与 5 日均量比量比
    取 bars 根（默认 61：10:30 时当日恰好 61 根 1 分钟线，不多不少不漏到昨日）。
    若扫描时点不是 10:30，bars 需同步调整，否则窗口会混入昨日尾盘。
    """
    out = {}
    if not _has("get_history"):
        return out
    i = 0
    total = len(codes)
    while i < total:
        chunk = list(codes[i:i + FETCH_BATCH])
        i += FETCH_BATCH
        raw = _hist_call(bars, "1m", fields, chunk)
        if not raw or not isinstance(raw, dict):
            continue
        for code, sub in raw.items():
            rows, idx = _sub_rows(sub, fields)
            if not rows:
                continue
            bar = {}
            price = _last_val(rows, idx, "close")
            if price is None:
                price = _last_val(rows, idx, "price")
            op = _last_val(rows, idx, "open")
            hi = _max_val(rows, idx, "high")
            lo = _min_val(rows, idx, "low")
            if price is not None:
                bar["price"] = price
            if op is not None:
                bar["open"] = op
            if hi is not None:
                bar["high"] = hi
            if lo is not None:
                bar["low"] = lo
            vsum = _sum_val(rows, idx, "volume")
            if vsum is not None:
                bar["volume"] = vsum
            if bar:
                out[str(code)] = bar
                out[_pure(code)] = bar
    return out


def _max_val(rows, idxmap, name):
    if not rows or not idxmap or name not in idxmap:
        return None
    j = idxmap[name]
    v = None
    for r in rows:
        try:
            x = float(r[j])
        except Exception:
            continue
        v = x if v is None else (x if x > v else v)
    return v


def _min_val(rows, idxmap, name):
    if not rows or not idxmap or name not in idxmap:
        return None
    j = idxmap[name]
    v = None
    for r in rows:
        try:
            x = float(r[j])
        except Exception:
            continue
        v = x if v is None else (x if x < v else v)
    return v


def _sum_val(rows, idxmap, name):
    if not rows or not idxmap or name not in idxmap:
        return None
    j = idxmap[name]
    s = 0.0
    got = False
    for r in rows:
        try:
            s += float(r[j])
            got = True
        except Exception:
            continue
    return s if got else None


def _diag_panel(panel, pool, fields, nbatch):
    """一次性 [诊断]：把「取数形态 + 为什么有效票是 0」一次查清，不靠猜。

    模仿 001 已被验证有效的做法（`[诊断] get_history 返回 type=...` /
    `[诊断] dict 共 N 键` / `[诊断] 首值 type=... repr=...`），并额外打印
    「解析出的列名」与「解出行数」—— 这两项是区分"没取到"与"取到了但解不出"的关键。
    """
    try:
        log.info("[诊断] 池=%d 分批=%d 批 形态=%s 合并后键数=%d(去重后代码数=%d) | 请求字段=%s"
                 % (len(pool), nbatch, _DS.get("style_name"),
                    len(panel) if hasattr(panel, "__len__") else -1,
                    len(set(_pure(k) for k in panel.keys())) if isinstance(panel, dict) else -1,
                    list(fields)))
        ks = list(panel.keys()) if isinstance(panel, dict) else []
        log.info("[诊断] 面板 keys 示例=%s" % (ks[:5] if ks else "空"))
        if ks:
            sub = panel[ks[0]]
            rows, idx = _sub_rows(sub, fields)
            try:
                ln = len(sub)
            except Exception:
                ln = -1
            log.info("[诊断] 首值 type=%s len=%s 解析列=%s 解出行数=%s"
                     % (type(sub).__name__, ln,
                        sorted(idx.keys()) if idx else None,
                        len(rows) if rows else 0))
            try:
                rep = repr(sub)[:240]
            except Exception:
                rep = "<repr失败>"
            log.info("[诊断] 首值 repr=%s" % rep)
        else:
            log.info("[诊断] 合并后面板为空 —— 形态=%s 四档已用尽；"
                     "下一步查 codes 元素是否带 .SS/.SZ 后缀" % _DS.get("style_name"))
    except Exception as e:
        log.error("[诊断] 诊断输出本身失败: %s" % e)



def _minute_last(code):
    """分钟线兜底：取当日最后一根分钟收盘价（日线不含当日时的最后手段）。"""
    try:
        if not _has("get_history"):
            return None
        raw = _hist_call(1, "1m", ["close"], [_suffix(code)])
        if raw is None:
            return None
        if isinstance(raw, dict):
            sub = raw[_suffix(code)] if _suffix(code) in raw else None
            if sub is None:
                sub = raw.get(_pure(code))
            if sub is None:
                vals = list(raw.values())
                sub = vals[0] if vals else None
        else:
            sub = raw
        rows, idx = _sub_rows(sub, ["close"])
        c = _col(rows, idx, "close")
        return c[-1] if c else None
    except Exception:
        return None


def _cur_price(data, code, hist_close=None):
    """取当日价：优先 data 对象，退化快照/昨收（hist_close 仅供风控巡检兜底）"""
    code_s = _suffix(code)
    bar = _cur_bar(data, code)
    if bar["price"] is not None:
        return bar["price"]
    try:
        snap = get_snapshot([code_s])
        if snap and code_s in snap:
            for f in ("last_px", "last_price", "price"):
                if f in snap[code_s] and snap[code_s][f] is not None:
                    return float(snap[code_s][f])
    except Exception:
        pass
    if hist_close is not None:
        return float(hist_close)
    return None


# ----------------------------- 板块/行业 -----------------------------------
def _parse_emb_sector():
    """v1.7 解析内嵌行业表 EMB_SECTOR_RAW -> ({code6: 板块名}, 板块数)。

    动机：外挂 sector_map.json 每次上传都要多带一个文件、还得赌只读目录路径
    对不对，路径不对就整层「行业均值 + 共振」静默失效（v1.4 踩过这个坑）。
    内嵌后单文件上传，零 IO、零路径依赖、零上传遗漏。

    串格式: "板块名:代码 代码 ...|板块名:..."（代码 6 位、空格分隔）。
    归属规则与 v1.4 一致：首次出现优先（顺序 = 生成时的板块顺序）。
    结果缓存到 g，避免逐票重复解析（5582 只 × 每票一次会很慢）。
    """
    g = _G()
    if g.get("emb_rev") is not None:
        return g["emb_rev"], g.get("emb_nb", 0)
    rev = {}
    nb = 0
    try:
        raw = EMB_SECTOR_RAW or ""
    except Exception:
        raw = ""
        log.error("[行业] EMB_SECTOR_RAW 未定义 -> 内嵌表缺失，将回落外挂文件")
    if raw:
        for seg in raw.split("|"):
            i = seg.find(":")
            if i <= 0:
                continue
            name = seg[:i].strip()
            if not name:
                continue
            nb += 1
            for c in seg[i + 1:].split():
                k = _pure(c)
                if k and k not in rev:
                    rev[k] = name
    g["emb_rev"] = rev
    g["emb_nb"] = nb
    return rev, nb


def _load_sector_index():
    """载入 sector_map.json -> {6位code: 行业名}，缓存到 g。

    【v1.4 根因修复】v1.3 的 _industry_of 只试 get_industry()，本平台没有这个接口
    → 恒返回 "" → 所有票归入同一个空行业 → 「行业均值排名 + 同行业共振>=2」整层
    失效（表现为 [统计] 里 板块强度Top=[('', (...,1,0))]，行业名是空字符串）。
    001 用的是外挂 sector_map.json（496 行业 + 504 概念 / 5645 标的），本函数照抄
    同款多路径 + 同款失败必告警纪律，不再静默返回空串。
    返回 {code6: 行业名} 反查表；失败返回 None（调用方据此打 error）。

    【v1.7】改为「内嵌优先、外挂兜底」：内嵌表 EMB_SECTOR_RAW 由
    gen_embedded_map.py 生成（416KB JSON -> 95KB 压缩串），单文件上传、零路径
    依赖。下面这段外挂多路径逻辑原样保留，仅在内嵌为空时启用。
    """
    g = _G()
    if g.get("ind_rev") is not None:
        return g["ind_rev"]
    # ① v1.7 内嵌表优先（零 IO / 零路径依赖 / 单文件上传不会遗漏）
    emb, emb_nb = _parse_emb_sector()
    if emb:
        g["ind_rev"] = emb
        g["ind_src"] = "内嵌"
        log.info("[行业] 内嵌表生效(申万一级) 行业数=%d 覆盖标的=%d | v1.8 内嵌"
                 "，零IO零路径依赖" % (emb_nb, len(emb)))
        return emb
    # ② 内嵌为空 -> 回落外挂文件（保留 v1.4 多路径逻辑作双保险）
    log.warning("[行业] 内嵌表为空 -> 回落外挂 %s（%d 个候选路径）"
                % (SECTOR_MAP_FILE, len(SECTOR_MAP_PATHS)))
    paths = list(SECTOR_MAP_PATHS)
    try:
        root = str(get_research_path()).rstrip("/")
        if root:
            paths = [root + "/只读/" + SECTOR_MAP_FILE,
                     root + "只读/" + SECTOR_MAP_FILE,
                     root + "/" + SECTOR_MAP_FILE] + paths
    except Exception:
        pass
    tried = []
    for path in paths:
        tried.append(path)
        try:
            with open(path, encoding="utf-8") as f:
                data = json.load(f)
        except Exception:
            continue
        ind = data.get("industry", {}) or {}
        if not ind:
            continue
        rev = {}
        for name, codes in ind.items():
            for c in codes:
                k = _pure(c)
                if k and k not in rev:
                    rev[k] = name
        if not rev:
            continue
        g["ind_rev"] = rev
        g["ind_src"] = "外挂"
        log.info("[行业] 外挂表加载成功 板块数=%d 覆盖标的=%d | source=%s 生成=%s 路径=%s"
                 % (len(ind), len(rev), data.get("source"),
                    data.get("generated_at"), path))
        return rev
    g["ind_rev"] = {}
    log.error("[行业] sector_map.json 载入失败（尝试 %d 个路径）→ 「行业均值+共振」"
              "整层将失效，候选会全部归入同一空行业" % len(tried))
    log.error("[行业] 已尝试路径: %s" % "; ".join(tried))
    log.error("[行业] 修复：把 sector_map.json 上传到 PTrade 只读目录，"
              "或把可用绝对路径加进 SECTOR_MAP_PATHS 后重跑")
    return g["ind_rev"]


def _industry_of(code):
    """行业归属：外挂 sector_map 反查 -> 平台接口兜底 -> ""（并计数，便于观测）。"""
    g = _G()
    rev = g.get("ind_rev")
    if rev is None:
        rev = _load_sector_index()
    name = rev.get(_pure(code)) if rev else None
    if name:
        return name
    try:
        if _has("get_industry"):
            ind = get_industry(_pure(code))
            if ind:
                return ind
    except Exception:
        pass
    g["ind_miss"] = g.get("ind_miss", 0) + 1
    return ""


def _get_market_pool():
    for fn in ("get_Ashares", "get_all_stocks"):
        if _has(fn):
            try:
                xs = globals()[fn]()
                if xs:
                    return [_suffix(x) for x in xs if _board(x) in ("MB", "CYB", "KCB")]
            except Exception:
                pass
    _warn("[扫描] 无可用股票池接口")
    return []


# ----------------------------- 初始化 ---------------------------------------
def initialize(context):
    g = {}
    globals()["g"] = g
    g["today"] = ""
    g["ind_rev"] = None         # code6 -> 行业名（v1.7 内嵌优先，未加载=None）
    g["ind_src"] = None         # 行业表来源：内嵌 / 外挂 / 空
    g["ind_miss"] = 0           # 查不到行业归属的累计次数（应远小于池子规模）
    g["entry_px"] = {}          # 自有成本（避摊薄成本陷阱）
    g["first_buy"] = {}         # 首次买入日
    g["hold_days"] = {}
    g["peak_px"] = {}           # 持仓峰值价
    g["winner_set"] = set()
    g["winner_peak_pnl"] = {}   # 赢家最高浮盈（百分点）
    g["partial_done"] = set()
    g["sector_stops"] = []      # (日期, 板块)
    g["sector_cooldown_days"] = {}
    g["cool_down"] = {}         # code -> 剩余冷静日
    g["noise_slot"] = {}
    g["buys_today"] = 0
    g["month_buys"] = {}
    g["turn_log"] = []          # 换手记账（买+卖双向）
    g["regime"] = "NORMAL"
    g["sentiment"] = "OK"
    g["sector_fade"] = {}       # 板块 -> 衰退计数
    g["candidates"] = []
    g["sector_strength"] = {}
    g["flat_days"] = 0          # 连续空仓交易日（空转看门狗）
    g["last_scan_ok"] = -9      # 上一轮扫描有效票数：-9 未跑 / -1 取数失败 / >=0 实际有效数
    log.info("[初始化] ★FUSION v1.8★ 启动；TRADE_ENABLED 见平台")
    log.info("[指纹] 热点=行业均值+强度分+共振>=2 | 止损=ATR clamp[6%%,11%%] | 大市=双弱HALT/单弱HALF(v1.6分层, REGIME_LAYERED=%s)" % REGIME_LAYERED)
    log.info("[指纹] v1.6 取数形态探测(is_dict=True/fq降级) + 结构化数组按dtype.names解析 + 字段=%s | 分批=%d"
             % (list(PANEL_FIELDS), FETCH_BATCH))
    log.info("[指纹] v1.6 当日价四层(data数值优先/日线price/分钟线/假设定价) 每层拒绝==昨收 | "
             "date列别名=%s | 行业=内嵌EMB_SECTOR_RAW优先/外挂%s兜底" % (list(DATE_COL_ALIASES), SECTOR_MAP_FILE))
    log.info("[指纹] v1.6 日线含当日与否由 date 列运行时判定（today=%s）；不含当日时按"
             "四层兜底，假设定价=昨收×%.3f" % (_context_date(context), 1.0 + ASSUME_ENTRY_PREMIUM))
    log.info("[指纹] v1.6 大市分层：双弱(1000&300同破MA20)HALT / 单弱HALF半仓 / 情绪冰点HALF"
             " | 变量 REGIME_LAYERED=%s（False 回退旧口径）" % REGIME_LAYERED)
    log.info("[指纹] v1.8 行业层=申万一级均值(全市场样本) 样本门槛>=%d 失效降级=%s | "
             "行业榜Top%d 共振>=%d | 入场: 涨幅主板%s 创科%s MA20缓冲=%.1f%% | 追高上限已对齐lhb888"
             % (SECTOR_MIN_STOCKS, SECTOR_CONFIRM_FALLBACK, TOP_INDUSTRY_N,
                MIN_SECTOR_ALIGN, _gain_range("600000.SS"), _gain_range("300001.SZ"),
                MA20_ENTRY_BUFFER * 100.0))
    # v1.7 启动即加载行业表并报来源（内嵌优先）；避免懒加载把 [行业] 行淹在后面
    _load_sector_index()


# ----------------------------- 盘前 -----------------------------------------
def before_trading_start(context, data):
    g = _G()
    g["today"] = _context_date(context)
    g["buys_today"] = 0
    g["candidates"] = []
    g["noise_slot"] = {}
    # 冷却衰减
    for k in list(g["cool_down"].keys()):
        g["cool_down"][k] -= 1
        if g["cool_down"][k] <= 0:
            del g["cool_down"][k]
    for k in list(g["sector_cooldown_days"].keys()):
        g["sector_cooldown_days"][k] -= 1
        if g["sector_cooldown_days"][k] <= 0:
            del g["sector_cooldown_days"][k]
    # 大市状态
    _update_regime(context)
    _update_sentiment(context)
    npos = len(_get_positions(context))
    log.info("[日初] 持仓 %d 只 | 大市=%s | 情绪=%s" %
             (npos, g["regime"], g["sentiment"]))
    # 空转看门狗：整段回测"0 成交"是 v1.0 踩过的坑（静默失败最难发现）
    if npos == 0:
        g["flat_days"] = g["flat_days"] + 1
    else:
        g["flat_days"] = 0
    if g["flat_days"] >= IDLE_WARN_DAYS and g["flat_days"] % IDLE_WARN_DAYS == 0:
        log.error("[空转告警] 连续 %d 个交易日 0 持仓 | 上一轮扫描有效票数=%s —— "
                  "若行情不至于此，先看 [扫描] 体检行与取数报错；"
                  "典型根因=字段名/取数口径与平台不符 → 候选池恒空 → 全天不开仓"
                  % (g["flat_days"], g["last_scan_ok"]))


def _context_date(context):
    try:
        dt = context.blotter.current_dt
        return str(dt)[:10]
    except Exception:
        try:
            return str(context.current_dt)[:10]
        except Exception:
            return ""


def _update_regime(context):
    """大市档位（v1.6 分层）。

    v1.5 及以前：中证1000<MA20 或 沪深300<MA20 → 记 HALT/PAUSE，但二者在买入闸门
    (见 _try_buy) 里行为**完全一样**（都只是"暂停新建仓"）→ 等价于
    「任一指数破 MA20 就全停」。实测 16/39 天(41%)被冻结，含 05-29~06-15 连续 13 天；
    而同期 lhb888 用「沪深300<MA20 才暂停」的口径正常交易并获利 —— 根因是单一
    高波动指数(中证1000)的 MA20 被反复穿越、频繁误触发。

    v1.6 改为分层：① 双弱(两指数同时破 MA20) → HALT 全停（真系统性风险）；
                  ② 单弱(仅其一破)           → HALF 半仓（降级但不缺席）；
                  ③ 都不弱                   → NORMAL。
    区间保护（双弱才全停）保留，日常误冻结去掉。REGIME_LAYERED=False 一键回退。
    """
    g = _G()
    small_below = False
    broad_below = False
    ok_small = False
    ok_broad = False
    ds = 0.0
    db = 0.0
    s_last = 0.0
    b_last = 0.0
    s_ma = None
    b_ma = None
    try:
        # v1.3：指数取数也走统一入口（旧版裸调 get_history 且 field 传字符串，
        # 在同一形态坑里同样会静默拿空 → 大市档位永远停在 NORMAL 假象）
        f_close = _norm_fields(["close"])
        raw, _nb = _fetch_panel(f_close, [REGIME_BROAD, REGIME_SMALL],
                                count=REGIME_MA + 5)

        def series(idx_code):
            if not raw:
                return None
            sub = raw.get(idx_code)
            if sub is None:
                sub = raw.get(_pure(idx_code))
            rows, idx = _sub_rows(sub, f_close)
            c = _col(rows, idx, "close")
            return c if c else None

        sb = series(REGIME_SMALL)
        br = series(REGIME_BROAD)
        if sb and len(sb) >= REGIME_MA:
            ok_small = True
            s_last = sb[-1]
            s_ma = _ma(sb, REGIME_MA)
            ma5 = _ma(sb[-REGIME_MA_FAST:], REGIME_MA_FAST) if len(sb) >= REGIME_MA_FAST else None
            if s_ma and s_last < s_ma:
                small_below = True
            if ma5 and s_ma and ma5 < s_ma and s_last < ma5:
                small_below = True
            if s_ma and s_ma > 0:
                ds = (s_last / s_ma - 1.0) * 100.0
        if br and len(br) >= REGIME_MA:
            ok_broad = True
            b_last = br[-1]
            b_ma = _ma(br, REGIME_MA)
            if b_ma and b_last < b_ma:
                broad_below = True
            if b_ma and b_ma > 0:
                db = (b_last / b_ma - 1.0) * 100.0

        # v1.6 自证行：把两指数的 close/MA20/偏离 打出来，否则"为什么HALT"永远是黑的
        log.info("[大市] 中证1000 close=%.2f MA20=%.2f 偏离=%+.2f%% 破MA20=%s | "
                 "沪深300 close=%.2f MA20=%.2f 偏离=%+.2f%% 破MA20=%s"
                 % (s_last, s_ma or 0.0, ds, small_below,
                    b_last, b_ma or 0.0, db, broad_below))
        if not ok_small or not ok_broad:
            log.error("[大市] 指数取数不全（中证1000命中=%s 沪深300命中=%s）→ 档位判定"
                      "不可信，本次按 NORMAL 处理但结论存疑" % (ok_small, ok_broad))
    except Exception as e:
        _warn("[regime] %s" % e)
        log.error("[大市] 档位判定异常 → 回退 NORMAL：%s" % e)

    if REGIME_LAYERED:
        if small_below and broad_below:
            g["regime"] = "HALT"
        elif small_below or broad_below:
            g["regime"] = "HALF"
        else:
            g["regime"] = "NORMAL"
    else:
        if small_below:
            g["regime"] = "HALT"
        elif broad_below:
            g["regime"] = "PAUSE"
        else:
            g["regime"] = "NORMAL"
    # 情绪冰点叠加 HALF（只在 NORMAL 时降级，避免把 HALF 再降一次）
    if g["sentiment"] == "ICE" and g["regime"] == "NORMAL":
        g["regime"] = "HALF"


def _update_sentiment(context):
    g = _G()
    g["sentiment"] = "OK"
    try:
        pool = _get_market_pool()
        if not pool:
            return
        # 口径：日线【不含当日】，末根=昨收，倒数第二根=前日收
        # => pct = 昨收/前日收-1 即「昨日涨跌幅」，与 1030/001 的情绪口径同源。
        # 只用 close 一个字段，规避 pre_close 这类非法字段名把整段取数打挂。
        fields = _norm_fields(["close"])
        raw, _nb = _fetch_panel(fields, pool, count=3)
        if not raw:
            # 【重要】取数失败时不能默默返回 OK —— 那会让情绪闸门假装"正常"，
            # 掩盖取数故障。这里显式标 FAIL，让 [日初] 行一眼看出情绪层没工作。
            g["sentiment"] = "FAIL"
            log.error("[senti] 分批取数返回空（%d 只）→ 情绪闸门失效，标记 FAIL" % len(pool))
            return
        zt = 0
        up = 0
        tot = 0
        for code in pool:
            try:
                sub = raw.get(code)
                if sub is None:
                    sub = raw.get(_suffix(code))
                if sub is None:
                    sub = raw.get(_pure(code))
                rows, idx = _sub_rows(sub, fields)
                closes = _col(rows, idx, "close")
                if not closes or len(closes) < 2:
                    continue
                pc = closes[-1]      # 昨收
                pc2 = closes[-2]     # 前日收
                if pc2 <= 0:
                    continue
                pct = (pc - pc2) / pc2
                tot += 1
                if pct >= _limit_pct(code):
                    zt += 1
                if pct > 0:
                    up += 1
            except Exception:
                continue
        if tot <= 0:
            g["sentiment"] = "FAIL"
            log.error("[senti] 面板有数据但一只都算不出涨跌幅 → 列名/口径对不上")
            return
        # 情绪口径自证：把样本量打出来，避免"样本 3 只也算出冰点"这类假信号
        g["senti_breadth"] = (zt, up, tot)
        log.info("[senti] 样本 %d 只 | 涨停 %d | 上涨占比 %.1f%%"
                 % (tot, zt, up * 100.0 / tot))
        if zt < MARKET_ZT_FLOOR:
            g["sentiment"] = "ICE"
        # 上涨占比过低也判冰点
        if up * 1.0 / tot < 0.30:
            g["sentiment"] = "ICE"
    except Exception as e:
        g["sentiment"] = "FAIL"
        _warn("[senti] %s" % e)


# ----------------------------- 扫描+选股 -------------------------------------
def scan_market(context, data=None):
    g = _G()
    pool = _get_market_pool()
    if not pool:
        return []
    # 批量取面板
    # 【v1.4】字段集 = PANEL_FIELDS（7 个，全部在平台白名单内）。除 001 实测的 5 个
    # OHLCV 外，补回 price（当日价第 2 档来源）与 money（成交额，免自算）。
    # 1030 策略在同平台长期稳定请求 price/money，故不增加取数风险。
    fields = _norm_fields(PANEL_FIELDS)
    panel, nbatch = _fetch_panel(fields, pool, diag=True)
    if not panel:
        g["last_scan_ok"] = -1        # -1 = 取数整体失败（区别于"取到但筛空"的 0）
        log.error("[扫描] 分批取数后面板为空（%d 批全部无数据，形态=%s）→ 全天不会开仓"
                  % (nbatch, _DS.get("style_name")))
        log.error("[扫描] 排查顺序: ①[取数]形态探测行是否命中（未命中说明四档全空）；"
                  "②codes 元素是否带 .SS/.SZ 后缀；"
                  "③[取数] 是否报了「无法识别的形态」（那就把该行发我补适配）")
        return []

    feats = {}
    miss_px = 0        # 当日价四层全空（不会被走到：最后一层是假设定价）
    fb_vol = 0         # 当日量不可信、退化用昨日量比的票数
    min_px = 0         # 靠分钟线兜底取到当日价的票数
    rows_short = 0     # 拿到数据但行数不足 MIN_HIST_BARS 的票数（诊断用）
    no_key = 0         # 面板里根本没有这只票的票数（诊断用）
    assumed_cnt = 0    # 用「保守假设定价」的票数（当日价前两层全空时）
    bar_today = 0      # 日线末根就是当日进行中 bar 的票数（口径自适应命中数）
    px_data = 0        # 当日价来源=data 对象（首选通道）
    px_field = 0       # 当日价来源=日线 price 字段
    px_minute = 0      # 当日价来源=分钟线
    date_ok = 0        # 成功取到 date 列（能真正判定当日口径）的票数
    # 当日 8 位整数日期（yyyyMMdd），与平台 K 线 date 列同口径 —— 用来运行时判定
    # 「末根是不是当日 bar」，避免继续用假设去赌平台口径。
    _ts = _context_date(context).replace("-", "")
    try:
        today_int = int(_ts) if _ts else 0
    except Exception:
        today_int = 0
    # 分钟线通道：本平台【回测里给的是昨日最后一根】（1030 日志第 15 行实证：
    # 「分钟close=11.13」== 昨收 11.13）。因此它只能当"最后兜底"，绝不能当
    # "当日价"用 —— v1.3 正是把当日价来源主要落在它身上，导致 pct 恒 0。
    snap_is_minute = False
    snap = _snap_map(pool)
    if not snap:
        snap = _minute_map(_norm_fields(["open", "high", "low", "close", "volume"]), pool)
        snap_is_minute = bool(snap)
    for code in pool:
        sub = panel.get(code) if isinstance(panel, dict) else None
        if sub is None:
            sub = panel.get(_suffix(code))
        if sub is None:
            sub = panel.get(_pure(code))
        if sub is None:
            no_key += 1
            continue
        try:
            rows, idx = _sub_rows(sub, fields)
            if not rows or len(rows) < MIN_HIST_BARS:
                rows_short += 1
                continue
            closes = _col(rows, idx, "close")
            opens = _col(rows, idx, "open")
            highs = _col(rows, idx, "high")
            lows = _col(rows, idx, "low")
            vols = _col(rows, idx, "volume")
            if not closes or not opens or not highs or not lows or not vols:
                continue
            # 【口径自适应】用返回的日期列判定末根是不是「当日进行中 bar」，
            # 不再靠猜（001 实测=含当日；1030 源码注释=不含当日，两者互相矛盾）：
            #   含当日 → 当日价/昨收/量比全部就地从末根拿，完全不依赖快照或分钟线；
            #   不含当日 → 末根=昨收，走 data/日线price/分钟线/假设定价四层兜底。
            # 【v1.4 date 列别名】平台结构化数组里这一列实际叫 datetime（不叫 date），
            # 旧写法查 "date" 恒取空 → has_today 恒 False（且日志照样写"不含当日"，
            # 属于假判定）。这里按别名表取，并把"取到列"与"判定结果"分开计数。
            dates = _col_first(rows, idx, DATE_COL_ALIASES)
            has_today = False
            if dates:
                date_ok += 1
                if today_int:
                    try:
                        dv = str(int(float(dates[-1])))
                    except Exception:
                        dv = str(dates[-1]).replace("-", "").split(".")[0]
                    try:
                        has_today = int(dv) == today_int
                    except Exception:
                        has_today = False
            if has_today:
                bar_today += 1
            prev_close = closes[-2] if (has_today and len(closes) >= 2) else closes[-1]
            if prev_close <= 0:
                continue
            assumed = False
            px_src_one = "日线末根"
            if has_today:
                cur = closes[-1]
                if cur <= 0:
                    continue
                cur_bar = {"price": cur, "open": opens[-1], "high": highs[-1],
                           "low": lows[-1], "volume": vols[-1]}
            else:
                # 【v1.4 根因修复】四层兜底 + 每层"与昨收相等即作废"的 guard：
                #   data 对象 -> 日线 price 字段 -> 分钟线 -> 保守假设定价。
                # 旧版只按字段名从 data 里读 price(拿到的是昨收)、且没有 guard，
                # 于是当日价恒=昨收 → pct 恒 0 → 涨幅区间全灭 → 全天 0 成交。
                dprice = _col_last(rows, idx, "price")
                mvals = snap.get(code) or snap.get(_pure(code)) or {}
                cur, px_src_one = _today_price(data, code, prev_close, dprice,
                                               mvals.get("price"))
                if px_src_one == "data":
                    px_data += 1
                elif px_src_one == "price字段":
                    px_field += 1
                elif px_src_one == "分钟线":
                    px_minute += 1
                    min_px += 1
                else:
                    assumed = True
                    assumed_cnt += 1
                cur_bar = _cur_bar(data, code, mvals)
                cur_bar["price"] = cur
                # 量比/日内形态只信「当日 bar」；当日 bar 不可得时全部退化到昨日口径
                if px_src_one != "data" or has_today:
                    cur_bar["open"] = closes[-1]
                    cur_bar["high"] = max(highs[-1], cur)
                    cur_bar["volume"] = None
                if cur is None or cur <= 0:
                    cur = prev_close * (1.0 + ASSUME_ENTRY_PREMIUM)
                    assumed = True
                    assumed_cnt += 1
                    px_src_one = "假设定价"
            pct = (cur - prev_close) / prev_close
            # 均线：日线含当日时末根本身就是进行中价，整体取均线即可；
            # 不含当日时，用「已收盘序列 + 当前价」补出含当日的短均线。
            if has_today:
                ma5 = _ma(closes, 5)
                ma10 = _ma(closes, 10)
            else:
                ma5 = _ma(closes[-4:] + [cur], 5) if len(closes) >= 4 else None
                ma10 = _ma(closes[-9:] + [cur], 10) if len(closes) >= 9 else None
            ma20 = _ma(closes, 20)
            ma60 = _ma(closes, 60)
            # 量比：只认「日线含当日」时的当日量；否则一律用【昨日量比】。
            # 【v1.4 口径修正】旧版把分钟线量当"当日累计量"，但本平台回测的分钟线
            # 给的是昨日（见 1030 日志），其窗口内求和 ≈ 昨日尾盘量 → 量比恒约 0.25，
            # 是所有票一起被系统性低估。既然不可信，就明确退化到昨日量比并计数。
            base5 = _ma(vols[-6:-1], 5) if has_today else _ma(vols[-5:], 5)
            if has_today and vols[-1] and base5:
                vol_ratio = vols[-1] / base5
            else:
                fb_vol += 1
                vol_ratio = (vols[-1] / base5) if (base5 and vols[-1]) else 1.0
            # 成交额：平台 money 字段优先（1030 同款做法），取不到再自算量×价
            money_today = _col_last(rows, idx, "money")
            if not money_today:
                money_today = (vols[-1] * cur) if (vols[-1] and cur > 0) else 0.0

            # v1.8：入场须站上 MA20 至少 MA20_ENTRY_BUFFER（lhb888 同款 2%）。
            # 贴线买入的票趋势不强，且与出场「破 MA20」挤在同一条线上，
            # 一有抖动就同时逼近买入与卖出条件。
            above20 = (ma20 is not None and cur >= ma20 * (1.0 + MA20_ENTRY_BUFFER))
            above60 = (ma60 is not None and cur >= ma60)
            shape = 0
            if ma5 and ma10 and ma20:
                if cur >= ma5 >= ma10 >= ma20:
                    shape = 3
                elif ma5 >= ma10:
                    shape = 2
            feats[code] = dict(pct=pct, vol_ratio=vol_ratio, above20=above20,
                               above60=above60, shape=shape, money=money_today,
                               day_open=cur_bar["open"], day_high=cur_bar["high"],
                               closes=closes, opens=opens, highs=highs, lows=lows,
                               last=cur, prev=prev_close, px_src=px_src_one)
        except Exception:
            continue
    if miss_px:
        g["diag_miss_px"] = g.get("diag_miss_px", 0) + miss_px
    if fb_vol:
        g["diag_fb_vol"] = g.get("diag_fb_vol", 0) + fb_vol
    # 取数体检：池子有效数 / 当日价来源分布 / 各档失败计数
    if snap_is_minute:
        px_src = "批量分钟线(回测=昨日,仅兜底)"
    elif snap:
        px_src = "批量快照"
    else:
        px_src = "无(仅data)"
    log.info("[扫描] 池%d 有效%d | 当日价: data%d 日线price%d 分钟线%d 假设%d | "
             "当日bar%d 量比退化%d | 无键%d 行数不足%d | 分钟通道=%s"
             % (len(pool), len(feats), px_data, px_field, px_minute, assumed_cnt,
                bar_today, fb_vol, no_key, rows_short, px_src))
    # ===== v1.4 关键自证行（照抄 1030 第 15 行的口径自检）=====
    # 这一行是本次三轮空转的"照妖镜"：它同时打出【昨收/当日价/口径来源】。
    # 若「data当日价」为空或等于昨收，说明 data 通道没取到 → 必定全天 0 成交。
    if not g.get("px_diag_done"):
        g["px_diag_done"] = True
        sample = None
        for c, ff in list(feats.items())[:1]:
            sample = (c, ff)
        if sample:
            log.info("[口径] 样本 %s 昨收=%.3f 现价=%.3f 涨幅=%.2f%% 成交额=%.2e "
                     "量比=%.2f 站MA20=%s | 当日价来源=%s | data通道命中%d/%d"
                     % (sample[0], sample[1]["prev"], sample[1]["last"],
                        sample[1]["pct"] * 100.0, sample[1]["money"],
                        sample[1]["vol_ratio"], sample[1]["above20"],
                        sample[1].get("px_src"), px_data, len(feats)))
    log.info("[口径] 形态=%s | 日线%s当日bar（date列命中%d/%d，today=%s）| 字段=%s"
             % (_DS.get("style_name"),
                "含" if bar_today else "不含", date_ok, len(feats), today_int,
                list(fields)))
    if date_ok == 0 and len(feats) > 0:
        log.error("[口径] date 列一只都没取到（别名表=%s）→ 无法判定日线是否含当日，"
                  "口径判定与量比/形态全部退化。请把 [诊断] 的「解析列」发我补别名"
                  % (list(DATE_COL_ALIASES),))
    if px_data == 0 and len(feats) > 0:
        log.error("[口径] data 通道一只当日价都没取到 → 涨幅将以日线末根口径退化，"
                  "若同时 date列命中0 则全部票的 pct 会退化为 0，导致粗筛必然全空。"
                  "排查：①handle_data 的 data 是否覆盖股票池（必要时 set_universe）；"
                  "②data 对象取值方式见 [口径] 样本行的「data通道命中」")
    if assumed_cnt:
        # 001 纪律：口径必须写进日志与结论，否则收益绝对值不可与真实快照口径相比
        log.info("[口径-假设] %d/%d 只按保守假设定价（成交价=昨收×%.3f）；"
                 "该假设下收益绝对值偏保守，不可与真实快照口径直接比较"
                 % (assumed_cnt, len(feats), 1.0 + ASSUME_ENTRY_PREMIUM))
    g["diag_bar_today"] = bar_today
    g["last_scan_ok"] = len(feats)
    if len(feats) == 0:
        log.error("[扫描] 有效票数为 0 —— 取数返回了但一只都用不了 | 无键%d 行数不足%d"
                  % (no_key, rows_short))
        log.error("[扫描] 判读: 无键大=合并后的键与池代码对不上（看 [诊断] 解析列）；"
                  "行数不足大=返回行数少于 MIN_HIST_BARS(%d)（调小或加大 count）"
                  % MIN_HIST_BARS)

    # 粗筛（v1.4：每一道闸都计数，粗筛为空时必须能一眼看出是哪道闸杀的）
    rough = []
    n_gain = 0
    n_amt = 0
    n_ma = 0
    samples = []
    for code, f in feats.items():
        lo, hi = _gain_range(code)
        if not (lo <= f["pct"] <= hi):
            n_gain += 1
            if len(samples) < 5:
                samples.append("%s pct=%.2f%%(需%.1f~%.1f%%)" % (_pure(code),
                               f["pct"] * 100.0, lo * 100.0, hi * 100.0))
            continue
        if f["money"] < MIN_AMOUNT:
            n_amt += 1
            if len(samples) < 5:
                samples.append("%s 额=%.2e(需%.1e)" % (_pure(code), f["money"], MIN_AMOUNT))
            continue
        if REQUIRE_ABOVE_MA20 and not f["above20"]:
            n_ma += 1
            if len(samples) < 5:
                samples.append("%s 未站MA20" % _pure(code))
            continue
        rough.append((code, f))
    log.info("[粗筛] 池%d → 涨幅区间淘汰%d → 成交额淘汰%d → MA20淘汰%d → 过关%d "
             "| 门槛: 额>=%.1e MA20=%s"
             % (len(feats), n_gain, n_amt, n_ma, len(rough), MIN_AMOUNT,
                REQUIRE_ABOVE_MA20))
    if not rough and samples:
        log.info("[粗筛] 淘汰样本: %s" % " | ".join(samples))
    if not rough:
        if n_gain >= len(feats) * 0.9:
            log.error("[粗筛] 九成以上票死在涨幅区间 → 当日价口径仍然不对"
                      "（pct 被压到 0 附近）。看上一行 [口径] 样本的「当日价来源」。")
        elif n_amt >= len(feats) * 0.9:
            log.error("[粗筛] 九成以上票死在成交额 → money 字段单位/口径不对"
                      "（看 [粗筛] 淘汰样本里的 额= 数量级）")
        return []

    # ---- 行业强度（v1.8 根因修复）----
    # v1.7 及以前：`for code, f in rough` —— 行业均值只遍历【候选池】，而候选已被
    #   涨幅区间截断在 3.5%~6%，各行业均值全挤在同一区间、排序接近随机，于是
    #   「行业榜 Top10」等于随机挑 10 个小板块 → 精选层被清空。实测：行业榜淘汰
    #   占全部淘汰 87.2%，候选中位数仅 2 只，5 天候选为 0。
    # v1.8：均值改用【全市场扫描池 feats】（与 lhb888 同口径），并用
    #   SECTOR_MIN_STOCKS 过滤小样本行业；行业层失效则降级不过滤（不误杀）。
    ind_pct = {}
    ind_zt = {}
    for code, f in feats.items():
        if f.get("px_src") == "假设定价":
            continue          # 假设定价的票口径不实，不参与行业均值
        ind = _industry_of(code)
        if not ind:
            continue
        if ind not in ind_pct:
            ind_pct[ind] = []
            ind_zt[ind] = 0
        ind_pct[ind].append(f["pct"])
        if f["pct"] >= _limit_pct(code):
            ind_zt[ind] += 1
    # 候选池的行业计数（共振口径不变：同行业【候选】>= MIN_SECTOR_ALIGN）
    ind_cnt = {}
    for code, f in rough:
        ind = _industry_of(code)
        ind_cnt[ind] = ind_cnt.get(ind, 0) + 1
    # 只保留样本充足的行业参与排名（lhb888 SECTOR_MIN_STOCKS）
    ind_avg = {}
    for k, v in ind_pct.items():
        if len(v) >= SECTOR_MIN_STOCKS:
            ind_avg[k] = _mean(v)
    strong_ind = set()
    for k, v in sorted(ind_avg.items(), key=lambda x: -x[1]):
        if v < INDUSTRY_MIN_RISE:
            continue
        if len(strong_ind) >= TOP_INDUSTRY_N:
            break
        strong_ind.add(k)
    # 降级保护：行业层无有效样本 -> 不做行业过滤（宁可少过滤，不可用错数据误杀）
    ind_filter_on = True
    if not ind_avg:
        if SECTOR_CONFIRM_FALLBACK:
            ind_filter_on = False
            strong_ind = set()
            log.error("[精选] 行业层无有效样本（有归属的行业数=%d 来源=%s 池=%d）"
                      " -> 降级为「不做行业过滤」（SECTOR_CONFIRM_FALLBACK=True）"
                      % (len(ind_pct), _G().get("ind_src"), len(feats)))

    cands = []
    # v1.6.1 精选漏斗计数（纯观测：定位「粗筛过关却买不到」是哪道闸最紧）
    n_sel_ind = 0
    n_sel_align = 0
    n_sel_ma60 = 0
    n_sel_shape = 0
    n_sel_pull = 0
    n_sel_open = 0
    n_sel_lu = 0
    for code, f in rough:
        ind = _industry_of(code)
        if ind_filter_on and ind not in strong_ind:
            n_sel_ind += 1
            continue  # 申万一级行业确认（lhb888）；行业层失效时降级为不过滤
        # 共振：同行业候选>=2（常开）
        if ind_cnt.get(ind, 0) < MIN_SECTOR_ALIGN:
            n_sel_align += 1
            continue
        # 细评：MA60 + 形态 + 日内回撤 + 回踩
        if REQUIRE_ABOVE_MA60 and not f["above60"]:
            n_sel_ma60 += 1
            continue
        if f["shape"] < MA_SHAPE_MIN:
            n_sel_shape += 1
            continue
        # 日内回撤 = (当日最高-当日价)/当日最高；当日最高不可得则跳过这道（宁可少过滤，不可用错数据误杀）
        dh = f["day_high"]
        if dh and dh > 0 and (dh - f["last"]) / dh > INTRADAY_PULLBACK_MAX:
            n_sel_pull += 1
            continue
        # 回踩：当日价 >= 当日开盘；开盘不可得则近似为「当日不破昨收」
        ref_open = f["day_open"] if f["day_open"] else f["prev"]
        if f["last"] < ref_open:
            n_sel_open += 1
            continue
        # 不买涨停
        if not ALLOW_LIMIT_UP_BUY and f["pct"] >= _limit_pct(code):
            n_sel_lu += 1
            continue
        # 强度分
        lb = 1 if f["pct"] >= _limit_pct(code) else 0
        score = f["pct"] + lb * 6.0 + min(f["vol_ratio"], 3.0) * 8.0 + f["shape"] * 1.5
        cands.append(dict(code=code, score=score, ind=ind, f=f))
    cands.sort(key=lambda x: -x["score"])
    cands = cands[:MAX_CANDIDATES]
    _ti = sorted(strong_ind, key=lambda k: -ind_avg.get(k, 0.0))[:3]
    _tt = ",".join("%s%.1f%%" % (k, ind_avg.get(k, 0.0) * 100.0) for k in _ti)
    log.info("[精选] 粗筛%d → 行业榜淘汰%d → 共振淘汰%d → MA60淘汰%d → 形态淘汰%d "
             "→ 回撤淘汰%d → 回踩淘汰%d → 涨停剔除%d → 候选%d(截前%d) "
             "| 行业榜%d/%d(全市场样本,门槛>=%d,过滤=%s,Top3=[%s])"
             % (len(rough), n_sel_ind, n_sel_align, n_sel_ma60, n_sel_shape,
                n_sel_pull, n_sel_open, n_sel_lu, len(cands), MAX_CANDIDATES,
                len(strong_ind), len(ind_avg), SECTOR_MIN_STOCKS,
                "ON" if ind_filter_on else "降级OFF", _tt))
    if not cands and rough:
        log.warning("[精选] 候选为0：粗筛过关%d 只全被精选层过滤 → 当日不建仓"
                    "（对照上方各道淘汰数，找最紧的一道）" % len(rough))
    g["candidates"] = cands
    g["sector_strength"] = {k: (ind_avg.get(k, 0), ind_cnt.get(k, 0), ind_zt.get(k, 0))
                            for k in strong_ind}
    return cands


# ----------------------------- 买入 -----------------------------------------
def buy_job(context, data=None):
    g = _G()
    cands = g.get("candidates") or []
    if not cands:
        log.info("[买入] 当日候选为0（精选层为空），跳过建仓")
        return
    if g["regime"] in ("HALT", "PAUSE"):
        log.info("[买入] 大市%s，暂停新建仓" % g["regime"])
        return
    total, cash = _account(context)
    if total <= 0:
        log.error("[买入] 账户总资产<=0（total=%.2f）→ 跳过建仓，请检查账户口径" % total)
        return
    budget_factor = SENTIMENT_HALF_FACTOR if g["regime"] == "HALF" else 1.0
    base_budget = min(cash * (1 - CASH_BUFFER), total * POSITION_VALUE_RATIO * MAX_POSITIONS)
    budget = base_budget * budget_factor
    live = _get_positions(context)
    live_codes = set(_pure(p["_code"]) for p in live)
    held = len(live)
    remaining = MAX_POSITIONS - held
    if remaining <= 0:
        log.info("[买入] 持仓已满(%d/%d)，跳过建仓" % (held, MAX_POSITIONS))
        return

    pending = []
    for c in cands:
        code = c["code"]
        if _pure(code) in live_codes:
            continue
        if _pure(code) in g["cool_down"]:
            continue
        if c["ind"] in g["sector_cooldown_days"]:
            continue
        if g["buys_today"] >= MAX_NEW_PER_DAY:
            break
        pending.append(c)

    for c in pending:
        if remaining <= 0 or budget <= 0:
            break
        if g["buys_today"] >= MAX_NEW_PER_DAY:
            break
        code = c["code"]
        # 强度加权分配
        weight = math.pow(max(c["score"], 0.01), STRENGTH_EXP)
        tot_w = sum(math.pow(max(x["score"], 0.01), STRENGTH_EXP) for x in pending)
        alloc = budget * (weight / tot_w) if tot_w > 0 else budget / max(len(pending), 1)
        alloc = min(alloc, total * MAX_SINGLE_RATIO)
        if alloc < 1000:
            continue
        # 双护栏：取当日价
        cur = _cur_price(data, code, hist_close=c["f"]["last"])
        if cur is None:
            cur = c["f"]["last"]
        if cur <= 0:
            continue
        # 最小 1 手闸门：分配额买不满 1 手会静默废单并白占预算
        lot = 200 if _pure(code).startswith("688") else 100
        if alloc < cur * lot * 1.02:
            continue
        lo_guard = cur * (1 - BUY_FLOOR_PCT)
        hi_guard = cur * (1 + BUY_PREMIUM_PCT)
        if cur < lo_guard or cur > hi_guard:
            continue  # 走弱不接刀 / 涨过头不追
        limit = min(cur * 1.02, hi_guard)
        try:
            order_target_value(_suffix(code), alloc, limit_price=limit)
            g["entry_px"][_pure(code)] = cur
            g["first_buy"][_pure(code)] = g["today"]
            g["peak_px"][_pure(code)] = cur
            g["buys_today"] += 1
            remaining -= 1
            budget -= alloc
            _record_turn("BUY")
            _record_month(g["today"])
            log.info("[买入] %s 行业=%s 强度%.1f 分配%.0f 限价%.2f" %
                     (code, c["ind"], c["score"], alloc, limit))
        except Exception as e:
            _warn("[买入] %s 下单失败: %s" % (code, e))


# ----------------------------- 风控/出场 ------------------------------------
def monitor_risk(context, data=None, tag="NORMAL"):
    g = _G()
    live = _get_positions(context)
    now = _now_hhmm(context)
    is_tail = now >= BREAK_MA_TIME
    for p in live:
        code = p["_code"]
        code6 = _pure(code)
        amount = _pos_field(p, ("amount", "current_amount"))
        enable = _pos_field(p, ("enable_amount",))
        if amount <= 0:
            continue
        cur = _cur_price(data, code)
        if cur is None:
            continue
        entry = g["entry_px"].get(code6)
        if entry is None or entry <= 0:
            entry = _pos_field(p, ("cost_basis", "cost_price", "avg_price")) or cur
            g["entry_px"][code6] = entry
        pnl = (cur - entry) / entry
        # 更新峰值
        if code6 not in g["peak_px"] or cur > g["peak_px"][code6]:
            g["peak_px"][code6] = cur
        peak = g["peak_px"][code6]
        peak_pnl = (peak - entry) / entry
        # 赢家粘性是否激活（浮盈>=门槛）：激活时豁免 ④移动止盈 / ⑦破均线
        winner_active = peak_pnl >= WINNER_EXEMPT_PCT
        # 必须每次重算：缓存会把建仓日的 0 固化，导致 held<1 恒成立、
        # T+1 判断永远跳过卖出分支（只买不卖）
        held = _days_between(g["first_buy"].get(code6, g["today"]), g["today"])
        g["hold_days"][code6] = held
        # T+1：当日买入不可卖，跳过所有卖出判定（防废单/噪声）
        if held < 1:
            continue

        # ① 硬止损（风险类，下限09:45）
        stop_pct = _stop_pct(code6, entry, cur)
        if now >= STOP_TIME_FLOOR and pnl <= -stop_pct:
            _sell_all(context, code, "硬止损", pnl=pnl, entry_today=(held < 1))
            _on_stop(code6, g, p)
            continue
        # ② 保本止损：峰值曾盈利>=3% 后回撤至成本线即退出（用 peak_pnl 判定，避免与 cur<=entry 互斥导致死代码）
        if peak_pnl >= BREAKEVEN_GUARD and cur <= entry:
            _sell_all(context, code, "保本止损", pnl=pnl, entry_today=(held < 1))
            continue
        # ③ 分批止盈（+10%减半，未做过）
        if pnl >= PARTIAL_TAKE_PCT and code6 not in g["partial_done"] and enable >= 100:
            _sell_half(context, code, p, pnl)
            g["partial_done"].add(code6)
            continue
        # ④ 移动止盈（武装5%/回撤15%，价格触发，无闸门）
        if (not winner_active) and peak_pnl >= TRAIL_ARM_PCT and cur <= peak * (1 - TRAIL_PCT):
            _sell_all(context, code, "移动止盈", pnl=pnl, entry_today=(held < 1))
            continue
        # ⑤ 赢家粘性锁利（百分点尺，自峰值回撤>=10pp；winner_active 已在循环头判定）
        if peak_pnl >= WINNER_EXEMPT_PCT:
            g["winner_set"].add(code6)
            g["winner_peak_pnl"][code6] = peak_pnl
        if code6 in g["winner_set"] and held <= WINNER_MAX_HOLD:
            giveback = peak_pnl - pnl
            if giveback >= WINNER_GIVEBACK_PCT:
                _sell_all(context, code, "赢家锁利", pnl=pnl, entry_today=(held < 1))
                continue
        # ⑥ +30%全清（锁利类，无闸门）
        if pnl >= TP_FULL_PCT:
            _sell_all(context, code, "止盈全清", pnl=pnl, entry_today=(held < 1))
            continue
        # ⑦ 破均线（趋势类，仅尾盘确认；赢家粘性激活时豁免）
        if (not winner_active) and is_tail:
            closes = _hist_closes(code, RETREAT_MA_HARD + 2)
            if closes and len(closes) >= RETREAT_MA_HARD:
                ma20 = _ma(closes, RETREAT_MA_HARD)
                if ma20 and cur < ma20:
                    _sell_all(context, code, "破20日线", pnl=pnl, entry_today=(held < 1))
                    continue
                ma10 = _ma(closes, RETREAT_MA_FAST)
                if ma10 and cur < ma10 and pnl < 0:
                    _sell_all(context, code, "破10日线", pnl=pnl, entry_today=(held < 1))
                    continue
        # ⑧ 时间止损（亏损持仓>=5日）
        if pnl < 0 and held >= MAX_HOLD_DAYS:
            _sell_all(context, code, "时间止损", pnl=pnl, entry_today=(held < 1))
            continue


def _stop_pct(code6, entry, cur):
    if STOP_MODE == "fixed":
        return FIXED_STOP_PCT
    # ATR 自适应（取近期振幅估算）
    closes = _hist_closes(code6, ATR_N + 2)
    if closes and len(closes) >= ATR_N:
        atr = _atr(closes, ATR_N)
        if atr > 0 and entry > 0:
            ratio = atr / entry
            pct = ATR_STOP_MULT * ratio
            return max(ATR_STOP_MIN_PCT, min(ATR_STOP_MAX_PCT, pct))
    return ATR_STOP_MIN_PCT


def _atr(closes, n):
    trs = []
    for i in range(1, len(closes)):
        trs.append(abs(closes[i] - closes[i - 1]))
    if len(trs) < n:
        return _mean(trs) if trs else 0.0
    return _mean(trs[-n:])


def _hist_closes(code, count):
    raw = _hist(code, count, "close")
    if raw is None:
        return None
    rows, idx = _sub_rows(raw, ["close"])
    return _col(rows, idx, "close")


def _sell_all(context, code, reason, pnl=0.0, entry_today=False):
    g = _G()
    code6 = _pure(code)
    try:
        order_target(_suffix(code), 0)
    except Exception as e:
        _warn("[卖出] %s 清仓失败: %s" % (code, e))
        return
    # 先快照成本算￥，再清（PTrade order 后持仓清零）
    entry = g["entry_px"].get(code6)
    enable = 0
    _record_turn("SELL")
    _record_trade(reason, pnl)
    log.info("[卖出] %s —— %s 浮盈%.1f%%" % (code, reason, pnl * 100))
    for k in ("entry_px", "first_buy", "peak_px", "hold_days", "winner_peak_pnl"):
        if isinstance(g.get(k), dict):
            g[k].pop(code6, None)
    for k in ("partial_done", "winner_set"):
        if isinstance(g.get(k), set):
            g[k].discard(code6)


def _sell_half(context, code, p, pnl):
    g = _G()
    code6 = _pure(code)
    amount = int(_pos_field(p, ("amount", "current_amount")))
    enable = int(_pos_field(p, ("enable_amount",)))
    lot = 200 if _is_kcb(code) else 100
    half = (amount // 2 // lot) * lot
    if half < lot or enable < half:
        return
    try:
        order(_suffix(code), -half)
    except Exception as e:
        _warn("[卖出] %s 减半失败: %s" % (code, e))
        return
    _record_turn("SELL")
    _record_trade("分批止盈", pnl)
    log.info("[卖出] %s 数量%d 减半 浮盈%.1f%%" % (code, half, pnl * 100))


def _on_stop(code6, g, p):
    ind = _industry_of(code6)
    g["sector_stops"].append((g["today"], ind))
    # 板块连败冷却
    recent = [s for s in g["sector_stops"]
              if s[1] == ind and _days_between(s[0], g["today"]) <= 20]
    if len(recent) >= 2:
        g["sector_cooldown_days"][ind] = 10
        log.info("[板块冷却] %s 连败>=2，冷却10日" % ind)


# ----------------------------- 换手/统计 -------------------------------------
def _record_turn(kind):
    g = _G()
    g["turn_log"].append((g["today"], kind))


def _record_month(today):
    g = _G()
    ym = today[:7]
    g["month_buys"][ym] = g["month_buys"].get(ym, 0) + 1


def _record_trade(reason, pnl):
    """记录每笔卖出（原因 + 浮盈率），供 [卖出] 分原因统计使用。"""
    g = _G()
    g.setdefault("trade_log", []).append((g.get("today", ""), reason, pnl))
    # 同步推进换手记账（卖出单向，配合 _record_turn 的买向构成双向）
    if reason not in ("",):
        g.setdefault("sell_reasons", {}).setdefault(reason, 0)
        g["sell_reasons"][reason] += 1


def _turnover_ok():
    g = _G()
    recent = [t for t in g["turn_log"] if _days_between(t[0], g["today"]) <= TURNOVER_WINDOW_DAYS]
    # 买+卖双向计数，按窗口折算年频次
    n = len(recent)
    span = max([_days_between(recent[0][0], g["today"]) for _ in recent] + [1])
    ann = n * 250.0 / max(span, TURNOVER_WINDOW_DAYS)
    if ann > MAX_ANNUAL_TURNS * 2:
        return False
    return True


def report_stats():
    g = _G()
    log.info("[统计] 大市=%s 情绪=%s 板块强度Top=%s 行业未命中=%d"
             % (g["regime"], g["sentiment"],
                sorted(g["sector_strength"].items(), key=lambda x: -x[1][0])[:3],
                g.get("ind_miss", 0)))
    # v1.6：空行业名按「占比」判定，不再一出现就 error。
    # v1.5 的写法只要有个别标的未命中(如 2 只)就报 error，属误报 —— 会把
    # 「sector_map 正常、仅个别新股/停牌股缺行业」误判成「整层失效」，
    # 白白吓人一轮（实测 行业未命中=2、Top3 全是真行业名，却报了 error）。
    try:
        ss = g.get("sector_strength") or {}
        tot = 0.0
        emp = 0.0
        for _k, _v in ss.items():
            cnt = _v[1] if (isinstance(_v, (list, tuple)) and len(_v) > 1) else 1
            tot += cnt
            if _k == "":
                emp += cnt
        if tot > 0 and emp > 0:
            share = emp / tot
            if share >= 0.30:
                log.error("[统计] 空行业名占 %.0f%%（%d/%d）→ 行业表大概率未生效，"
                          "「行业均值+共振」失效。见上方 [行业] 行"
                          % (share * 100.0, int(emp), int(tot)))
            else:
                _warn("[统计] 空行业名 %d 只（占 %.1f%%，已忽略）→ sector_map 基本正常，"
                      "多半是个别新股/停牌股不在表里" % (int(emp), share * 100.0))
    except Exception as e:
        _warn("[统计] 行业占比自检异常：%s" % e)


def log_net_value(context, data=None):
    """逐日净值快照（v1.5 自算持仓市值，修复 HALT/PAUSE 日平台不给持仓定价→市值记0）。

    口径与 热点早盘001 `[净值] 日期 总资产=N 持仓=M 只` 对齐，使 compare_all.py
    可直接入库做同口径横向比较。compare_all.py 抓的是 `总资产=` 字段，故本函数让
    `总资产=` 输出【自算修正权益】(现金 + 持仓市值)，HALT 日才准；原平台
    portfolio_value 另存 `平台账=` 供对照。持仓市值用 v1.4 已验证的 _cur_price
    （数值优先当日价）自算，取不到时退化用成本价，避免市值恒0的假象。
    本函数只读账户、不下单。
    """
    g = _G()
    total, cash = _account(context)
    pos_value = 0.0
    npos = 0
    try:
        live = _get_positions(context)
        for p in live:
            code = p["_code"]
            amount = _pos_field(p, ("amount", "current_amount"))
            if amount is None or amount <= 0:
                continue
            npos += 1
            cur = _cur_price(data, code) if data is not None else None
            if cur is None or cur <= 0:
                # HALT 日 data 可能也无当日价 -> 退化用成本价估值，至少不是 0
                cur = _pos_field(p, ("cost_basis", "cost_price", "avg_price")) or 0.0
            pos_value += amount * cur
    except Exception as e:
        _warn("[净值] 持仓估值异常: %s" % e)
    equity = cash + pos_value
    flag = " (HALT日成本补齐)" if (npos > 0 and pos_value <= 0) else ""
    log.info("[净值] %s 总资产=%.0f 现金=%.0f 持仓=%d 只 持仓市值=%.0f 平台账=%.0f%s"
             % (g["today"], equity, cash, npos, pos_value, total, flag))


# ----------------------------- 时钟/入口 ------------------------------------
def _now_hhmm(context):
    try:
        dt = context.blotter.current_dt
        return str(dt)[11:16]
    except Exception:
        return "15:00"


def handle_data(context, data):
    now = _now_hhmm(context)
    g = _G()
    g["today"] = _context_date(context)
    if now == SIGNAL_TIME:
        cands = scan_market(context, data)
        buy_job(context, data)
        report_stats()
    elif now in RISK_TIMES:
        monitor_risk(context, data, "NORMAL")
    elif now in TAIL_RISK_TIMES:
        monitor_risk(context, data, "TAIL")
    # 逐日收盘净值快照（放在最后，任何分支都执行；仅记录，不影响交易）
    if now == NET_VALUE_TIME:
        log_net_value(context, data)


# 若平台用 run_daily 而非 handle_data，可注册：
# run_daily(before_trading_start, 'before_open')
# run_daily(lambda c,d: handle_data(c,d), SIGNAL_TIME)
