# 国金 PTrade 三层策略 —— 上线前核对清单

> 本策略文件：`国金PTrade_三层量化策略.py`
> 平台：国金证券 PTrade（恒生系白标，Python 3.5 托管环境）
> 默认 `TRADE_ENABLED = False`，先信号模式跑 3~5 个交易日核对日志，再模拟盘，最后小资金实盘。
> **API 口径已定稿（2026-09-21）**：所有接口已对照官方《国金PTrade的API文档.docx》（提取文本 `国金PTrade_API文档_提取.txt`）逐项核对并固化，原 `[需确认]` 项全部闭环；修正了 6 处致命/静默错误（见下方表格）。

---

## 一、必须逐条核对的平台接口口径（猜错 = 静默错）

| # | 项 | 本策略写法（已定稿） | 核对结果（2026-09-21 对照 国金PTrade_API文档_提取.txt） |
|---|---|---|---|
| 1 | 股票代码后缀 | `.SS/.SZ/.BJ`（由 `_suffix` 自动补；指数 `000300.SS` 原样） | 【已确认】文档示例 `set_universe('600570.SS')` 即用此后缀 |
| 2 | 历史行情 `get_history` | `get_history(count, frequency, field, security_list)`，单只字符串返回 DataFrame（列=字段名），**无 `df` 参数**；`_hist_lists`/`_vwap` 已按此定稿 | 【已确认】文档示例 `get_history(5,'1d','close',security,fq=None,include=False)`：参数顺序 (count,频率,字段,标的)，返回 DataFrame，`df['close']` 可取值 |
| 3 | 持仓字段 | `get_positions()` 返回 list[dict]，经 `_pos_to_dict` 映射 `current_amount`/`enable_amount`/`cost_price`/`current_price`(←`last_price`)；6 位无后缀 `stock_code` 统一补后缀 | 【已确认】文档示例返回 `stock_code`(6位无后缀)/`current_amount`/`enable_amount`/`last_price`/`cost_price`；`enable_amount` 确为 T+1 可卖量（文档提示类型可能为 str/float/int，已用 `_f()` 兼容） |
| 4 | 账户信息 | PTrade **无 `get_account()`**；改用 `context.portfolio` 的 `portfolio_value`/`cash`/`positions_value`，经 `_account()` 返回 `{total_value,...}` | 【已确认·已订正】文档示例 `context.portfolio.cash`；无 `get_account`，账户信息在 `context.portfolio` |
| 5 | 下单 `order_target_value` | `order_target_value(code, value)`，`value`=目标市值 | 【已确认】文档接口表列有 `order_target_value(回测/交易)`，支持该签名 |
| 6 | 盘中现价 | `data[code].price` → `obj['close']` → `get_snapshot(code)` 的 `last_px`（快照为交易时段接口） | 【已确认·已订正】文档示例 `data[g.security].price` 与 `data[sid]['close']`；快照最新价字段为 `last_px`；`data.current()` 是聚宽语法，PTrade 不存在（原写法会 AttributeError，已改） |
| 7 | 分钟线 | `_vwap` 用 `get_history(T_VWAP_WIN,'1m',["close","volume"],code)`，同分钟缓存 | 【已确认】文档 `get_history` 支持研究/回测/交易，含 1m 频率 |
| 8 | 回调名 / 时钟 | `initialize`/`before_trading_start`/`handle_data`/`after_trading_end`；时钟用 `context.blotter.current_dt`（`_dt_now` 兼容退化 `context.current_dt`） | 【已确认·已订正】文档示例 `context.blotter.current_dt.strftime('%Y-%m-%d')`（handle_data 内）；`context.current_dt` 不可靠 |
| 9 | 指数代码 | `000300.SS`（沪深300） | 【已确认】`get_history` 可对指数返回，沪深300 用 `000300.SS` |

---

## 二、本策略已落实的硬规则（对照 ptrade-strategy-dev 技能）

- [x] Python 3.5 兼容：无 f-string、无类型注解、无 `os` 模块、无外网请求
- [x] `initialize` 内 `set_universe(STOCKS)`：PTrade 框架要求注册标的后 `handle_data` 的 `data` 才带个股行情（已补，缺则 L3 日内T 整段失效）
- [x] T+1：所有卖出走 `enable_amount`，`enable_amount==0` 不回退到 `current_amount`（不发废单）
- [x] 涨跌停按板区分：`_plate_pct` 对 30/68→20%、8/4→30%、ST→5%、其余 10%（L3 卖在涨停附近会被 `enable_amount`/限价自然挡住；如需显式跳过涨停可加 `_near_limit` 判断）
- [x] 成本含印花税：单边 0.05%（2023-08-28 起），佣金双边 0.025%，滑点 0.05%；`COST_PER_TURN` 参与换手预算
- [x] 回测当日价口径：L1/L2 信号在 `before_trading_start` 用**已完成数据**（末根=昨收）；L3 盘中价显式标注来源 `[口径]`
- [x] 盈亏比自洽：`L2_STOP_PCT(7%) <= L2_TAKE_PCT(14%) * 0.5` → 自检打印 PASS
- [x] 出场分级闸门：风险类（L2 硬止损）只设下限 09:45；趋势类（L2 止盈/超买/移动止盈）盘前用完成数据执行
- [x] 两段式止损已**弃用**：统一单段一次性清仓 + 策略自有成本价 `entry_px`（防平台摊薄口径改写）
- [x] 组合级风控：三级断路器（-8%/-12%/-18%）+ 沪深300 均线市场开关；断路器在 `_do_buy_value` 中**独立拦截所有方向性买入**（L1 季度再平衡补仓也受控；L3 日内T回补不走此函数，不受影响）
- [x] 市场开关**双向可复位**（2026-09-21）：短均线(60)回归即恢复 L2 新开；长均线(120)回归且连续 `MKT_RESET_CONFIRM_DAYS` 日（默认 1）后档位复位为 0，再次跌破可再减仓；均线数据缺失时维持当前档位、不误复位（`_index_below_ma` 三态返回）
- [x] 换手预算：`MAX_ANNUAL_TURNS` 按"年度成本预算 ÷ 单回合成本"换算，买卖双侧记账，超阈暂停新开
- [x] 任何接口降级都 `log.warning`，绝不静默换口径（`_hist_lists` / `_cur_price` / `_get_positions` / `_account` 全有告警）
- [x] L3 日内T 闭环：只卖可卖量、当日必回补（`14:50` 强制平），**不增隔夜仓、不碰 L1 核心**
- [x] 行业上限约束（SECTOR_MAX=30%）：在 `_do_buy_value`（L1/L2 建仓唯一入口）下单前，按 `SECTOR_OF` 聚合所属细分行业当前总市值占比，超阈**拒单/减额**；L3 日内T 走独立闭环（隔夜净仓不变）豁免，避免误杀。未配置行业的标的跳过约束但 `[sector-cap]` 告警一次
- [x] L1 最短持有保护（L1_MIN_HOLD_DAYS=5）：`_l1_rebalance` 追买前若 `_held_days(cd) < 5` 则跳过该票再平衡追买（防过度换手）；仅约束 L1 再平衡加仓，L2 从空仓建仓不受影响

---

## 三、上线前手动核对动作

1. **信号模式跑 3~5 日**：`TRADE_ENABLED=False`，看日志是否出现预期 `[signal-buy]`/`[信号-卖]`，以及 `[口径-降级]`（若 L3 关说明回测无盘中价，正常）。
2. **核对 T+1 拦截**：制造"当日买入"场景，确认日志打印 `可卖为0(T+1未交收)...顺延次日`，无废单。
3. **核对成本预算告警**：制造高频触发，确认 `[换手预算]` 告警并产生暂停。
4. **核对断路器**：模拟组合回撤 >8%/12%/18%，确认分级减仓日志与动作。
5. **小资金实盘**：`TRADE_ENABLED=True`，单票极小仓，盯 1~2 周无异常再放大。
6. **L1 季度再平衡**只在第 60 个交易日触发一次，确认不会每天调仓（避免把长线核心做成短线）。
7. **核对行业上限**：制造某细分行业（如"集成电路设计"=兆易+澜起）已接近 30% 场景，确认日志打印 `[sector-cap]` 减额/拒单；L3 日内T 不受影响。
8. **核对断路器拦 L1**：触发 -8% 断路器后，确认 L1 季度再平衡不再补仓（日志无 `[signal-buy]`，或仅有 L3 回补）；熔断解除（回撤收敛）后恢复。
9. **核对 L1 最短持有**：对某已持仓 <5 日的票制造"低配"场景，确认日志打印 `[L1] ... 持仓不足5日，跳过再平衡追买`，不追买。
10. **核对市场开关复位**：确认指数回到 120 日线上方连续 `MKT_RESET_CONFIRM_DAYS` 日后打印档位复位日志，且此后**再次跌破** 120 日线时能**再砍两成**（验证单向闩锁已解除）。

---

## 四、已知局限 / 待你确认

- `SECTOR_OF` 已按原表「自选股20260920.xls」的 `细分行业` 字段逐只填充（16 只全覆盖），并在 `_do_buy_value` 中**已生效**做行业上限拦截；如平台有 `get_industry` 可替换为动态取值并双写 key（纯数字 + 带后缀）。
- `_held_days` 已改为用 `g["buy_day"][cd]`（建仓日 trade_days）与当前 `trade_days` 做差，拿不到记录视作长持(9999)；不再恒返回 99。
- 印花税 0.05%（2023-08-28 起减半）/ 佣金 0.025%（万2.5）/ 滑点 0.05%（回测建模假设）**已按现行费率定稿**（2026-09-21）；过户费（中登万0.1双向）未含，真实成本略高。
- L3 日内T 在"回测无盘中 data"环境下自动关闭，实盘需确认 `handle_data` 能拿到实时 `data` 或 `get_snapshot` 可用。
- 市场开关复位去抖：`MKT_RESET_CONFIRM_DAYS` 默认 1（即回到 120 日线上方就复位）。若回测显示指数在 120 日线附近反复穿越、引起反复减仓，可调大到 3~5 提高复位门槛。

> ⚠️ 本报告基于 2026-09-20 快照的量化+定性研究，**非买卖建议**。自动化策略仍需人工兜底开关：极端行情、停牌、平台故障、接口字段变更都需人工介入。

---

## 附录：首跑回测复盘（2026-09-21）

详见同目录 `首跑回测复盘_20260921.md`。要点：

- 首跑为**信号模式**（`TRADE_ENABLED=False`），零成交，**收益与风控未验证**；仅通过"接口 + 信号"冒烟测试。
- 首跑暴露并已修复 6 项缺陷：换手预算误拦 L1 建仓（回归）、换手计数口径错、年化外推早期失真、告警同秒重复、L3 不受换手预算约束、市场开关档位单向闩锁（已改双向复位）。
- 新增常量 `TURNOVER_MIN_DAYS = 20`；`_record_turn(reason, round_trip=False)` 双口径。
- **重跑必须设 `TRADE_ENABLED = True`**，否则依旧无法验证盈亏与断路器。
- 已闭环（2026-09-21）：`_market_switch` 的 `mkt_stage` 已改为**双向复位**——指数回到 120 日线上方连续 `MKT_RESET_CONFIRM_DAYS` 日后档位归零，再次跌破可再减仓；复位只复位档位标记，不回补已砍仓位（与断路器口径一致）。
- 待核实：回测复权口径（690012/688111 价格与实盘差异较大）。
- **★P0 已修（2026-09-21）**：对照《山西证券的API文档》复核时发现 `get_positions()` 官方口径为 **`dict[str:Position]`**（返回 `list[dict]` 的是 `get_all_positions()`，仅交易模块）。原 `_get_positions()` 写成 `for p in poss`，遍历 dict 只得字符串键 → 持仓**全部被静默丢弃** → 止盈/止损/L3日内T/行业上限**全部失效**。
  已在**国金与山西两版同步修复**：改用 `dict.items()` 取键值对、保留 `context.portfolio.positions` 退化通道、`_pos_to_dict(p, code_hint)` 用字典键兜底代码。
  **重跑核对项**：`TRADE_ENABLED=True` 后必须确认 `_daily_risk_scan` / `_t_once` 能识别真实持仓（而非恒空）。
  详见山西目录 `上线前核对清单_山西PTrade.md` §六。
