# 热点早盘001 V4.9 ——「收益下降」版本误判说明

> 诊断日期：2026-09-20
> 一句话结论：**你拿到的那份回测日志是 V4.5 跑的，不是 V4.9。V4.9 至今一次都没真正跑过，"收益下降"是个误判。**

---

## 一、用户反馈

用户上传截图 + `001优化方案回测.txt`，称"V4.9 收益反而下降了"。

## 二、实际跑的是哪个版本？

对 `001优化方案回测.txt` 逐字比对 + 用 `工具-回测日志自检.py` 自动判定，结论：**判定来源版本 = V4.5（匹配分 15），13/13 项与 V4.9 不符。**

### 铁证（白纸黑字）

日志第一行初始化就打印：

```
[初始化] V4.5 启动 TRADE_ENABLED=True ...
[初始化] 每板块限 2 只 | 总仓上限 6 只 | 单票启动15%/扩散10% | 每日新建≤2 笔
[初始化] 止损模式=atr | 移动止盈 武装5%/回撤10% | 破10日线
[初始化] 换手预算: 近20交易日 ≤30 次双边 | 每月建仓≤20 笔 | 产业链簇 Jaccard>0.3 同簇≤40%
```

而 V4.9 这些参数**已全部改掉**（见下表），且 V4.9 的启动日志会额外打印 `[指纹]` / `★V4.9 止损形态★` / `赢家宽管` / `市场避险` / `[口径-探测]` —— **这份日志里这些全是 0 条**。

### 关键参数对照

| 参数 | 日志实际 | V4.9 应为 | 指向 |
|---|---|---|---|
| 打印版本串 | `V4.5 启动` | `V4.9 启动` | V4.5 |
| `[指纹]` 行 | 无 | 有 | 非 V4.9 |
| 移动止盈回撤 | 10% | **15%** | V4.5 |
| 分批止盈阈值 | 12.5%（实测 6 笔） | **15%** | V4.5 |
| 每板块限仓 | 2 只 | **3 只** | V4.5 |
| 扩散期单票仓位 | 10% | **12%** | V4.5 |
| 产业链簇 Jaccard | 0.3 | **0.5** | V4.5 |
| 换手窗口/上限 | 20 日 / 30 | **60 日 / 6** | V4.5 |
| `[净值]` 日终行 | 0（V4.5 不打印） | 39 条 | 非 V4.7+ |
| 赢家宽管 / 市场避险 / 口径-探测 | 0 / 0 / 0 | 应有 | 非 V4.9 |

## 三、为什么会有"下降"的错觉

- V4.5 区间收益 ≈ **−2.6%**（反推，见 `热点早盘001_V4.5-回测复盘与结论.md`）。
- V4.8（上一真实跑版）面板 **−1.90%**。
- 把 V4.5 的结果当"V4.9"去比 V4.8，自然看到"又降了"。**根因是跑了旧文件，不是 V4.9 改坏了。**

## 四、正确做法（重跑 V4.9）

1. **在 PTrade 平台里确认选中的文件是 `热点早盘001_V4.9.py`**（MD5 `5face8a6923c`）。平台可能存在文件选择错位 / 旧文件缓存，务必肉眼确认文件名。
2. 跑完后**先看启动第一屏**：应出现 `[初始化] V4.9 启动` 和一行 `[指纹] ★V4.9★ 止损=atr ... 止盈:分批15% 移动武装5%/回撤15% ...`。
3. **跑完用自检工具核对**，必须 0 不符才算数：
   ```
   cd "D:\投研工作台\06-投资策略分析\策略回测\热点早盘001"
   python 工具-回测日志自检.py
   ```
   输出"不符项: 0 / 13"才可信；否则就是又跑错文件了。
4. 回测区间请沿用 **2026-05-06 ~ 06-30（39 交易日）**，与 V4.5~V4.8 同区间可比。

## 五、V4.9 到底改了什么（提醒自己别被旧日志误导）

- P0-3 **赢家宽管**：浮盈≥15% → 移动止盈回撤放宽到 24%、趋势线 MA10→MA20、最长宽管 30 日。
- P0-2 **主线分散**：`MAINLINE_MAX_ENTRIES=4`（滚动 20 交易日）。
- P1-1 **阈值重置**：止损 0.12→**0.06**、止盈 0.10→**0.15**、TRAIL 0.10→**0.15**（目标盈亏比 0.83→2.50）。
- 判据改用自有成本 `g.entry_px`（治平台 `cost_price` 摊薄），删两段式止损，出场理由统一补浮盈亏。

## 六、防复发

`工具-回测日志自检.py` 已沉淀为通用工具：传一份日志 + 一份目标 .py，自动比对 13 个项目给出"是否跑对版本"的判定。**每次回测完先跑它，再下任何"变好/变坏"的结论。**
